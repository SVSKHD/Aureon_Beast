# Phase 1 Decisions

Ambiguities in ARCHITECTURE.md resolved by the simplest reading. Each is reversible in Phase 1 without touching services.

| # | Spec | Decision |
|---|------|----------|
| 1 | §25 lists states but not every edge | `REQUESTED → CANCELLED/EXPIRED` (wizard abandoned or timed out); `CONFIRMED → CANCELLED` (requester cancels before the executor claims). Nothing may return to `REQUESTED` or `CONFIRMED`. `FAILED_RECONCILIATION` is terminal — manual repair is an operator action outside the state machine, audited separately. |
| 2 | §41 `FAILED_SPREAD_LIMIT` "possible state/error" | Modelled as `status=FAILED` + `failure_code=SPREAD_LIMIT`, not a separate state. Keeps the state graph small; `FailureCode` enum carries the specifics (§56, §78). |
| 3 | §12 hash separator | ASCII unit separator 0x1F, because `event_key` legitimately contains `\|`. |
| 4 | §12 `account_scope` | Config `AUREON_ACCOUNT_SCOPE`, default `"primary"`. Deliberately an alias, not the broker login, so a broker migration doesn't re-key history. **Set this before Phase 2 and never change it.** |
| 5 | §34 comment token | `AUR:` + 6 base32 chars from sha256(request_id) = 10 chars total; deterministic so a crashed executor can re-derive it. |
| 6 | §8 market timezone default | `Europe/Athens` (typical MT5 server clock, DST-aware). Override with `AUREON_MARKET_TZ`. |
| 7 | §18 session times | Placeholder defaults in `config/sessions.py` (Asia 02–10, London 10–18, NY 15–23 market time). Overlap resolved by fixed precedence London → NY → Asia. Bump `SESSION_CONFIG_VERSION` on any change. |
| 8 | §50 inferred links | `TradeRequest` and `Trade` reject `link_type=inferred`; inferred relationships live only in review documents (Phase 7). |
| 9 | §43 pending orders | No separate `pending_orders` collection; `PendingOrder` is a model the monitor uses over the `PENDING` trade request. Add a collection later only if queries need it. |
| 10 | §67 heartbeats | Stored both embedded in `system_state` (for one-read status) and in a `heartbeats/{service}` doc (source of truth the monitor writes). |
| 11 | §56 settings | Firestore `settings/execution` (`ExecutionSettings`) overrides env defaults at runtime; `trading_enabled` defaults to **false**. |
| 12 | §6 schema version | Single integer `schema_version = 1` on every document; per-collection versions can be introduced later without migration. |
| 13 | §22 evaluation status | `INVALID` reserved for horizons that cannot complete (e.g. data gap); a `COMPLETE` horizon must carry all excursion fields (validator). |
| 14 | §23 `reached_N` / `time_to_N` | Stored as `reached: dict[threshold, bool]` and `time_to: dict[threshold, seconds\|None]` rather than 10 named fields, so thresholds are configurable per rule. |
| 15 | Trade `close_reason` | Free string constrained by convention (`sl\|tp\|manual\|mobile\|broker\|discord\|unknown`); made an enum once Phase 5 shows what MT5 actually reports. |

## Config keys required by §84 — where they live

| Key | Location |
|-----|----------|
| confirmation_ttl_seconds, quote_ttl_seconds, status_stale_after_seconds | `AureonConfig` (env) and `ExecutionSettings` (Firestore, wins at runtime) |
| max_deviation_points, max_spread_points, executor_lease_seconds | same |
| agent_version, agent_params_snapshot | per-agent, stamped on each `Detection` |
| session_config_version | `config/sessions.py` |
| schema_version | `models/base.py` |
| evaluation_rule_id | `AureonConfig.evaluation_rule_id` |
| aureon_magic | `AureonConfig.aureon_magic` |

---

## Decisions added while implementing Phase 1

Same rule as above: simplest reading, reversible within Phase 1. Rows 17 and 22
are the two that want confirmation against the frozen spec, because they were
resolved without the section text in front of me.

| # | Spec | Decision |
|---|------|----------|
| 16 | §12 `detection_id` components | Hashed tuple is `(account_scope, symbol, timeframe, agent_name, event_key, candle_open_time)`. **`agent_version` is deliberately excluded**: a patch release re-running over history must upsert the SAME document, not mint a parallel detection for a candle that only happened once. The version is still stamped as a field, so "which build saw this" stays answerable. |
| 17 | §61/§63 review fields | Section text unavailable; modelled a superset — `detections_by_agent`, `detections_by_session`, `HorizonOutcome`/`ThresholdOutcome` per horizon and threshold, plus `pending_horizons_excluded` and `invalid_horizons_excluded`. **Confirm against §61/§63 before Phase 7 aggregates against it.** |
| 18 | §84 env parsing | An unset **or empty** variable falls back to its default (ordinary env convention). A **whitespace-only** `account_scope` is rejected instead, because it would key every detection id in history on `" "`. |
| 19 | §59 `system_state` location | Single document at `system_state/current` rather than one per symbol. Per-symbol state is the `symbols[]` array inside it, so `/status` and the dashboard render from one read (and one realtime listener). |
| 20 | §18 `sessions/` doc id | `{market_date}__{session}` — broker-local date, so re-running a session overwrites rather than duplicating. |
| 21 | §18 session boundaries | Windows are half-open `[start, end)`. With Asia ending and London starting at 10:00, 10:00 belongs to London alone; without this a boundary candle would fall in two sessions. |
| 22 | §22 `detection_id` length | Full sha256 hex (64 chars). Firestore's 1500-byte id limit is not a constraint, and truncating trades collision margin for cosmetics. |
| 23 | §23 hit rate with no data | `ThresholdOutcome.rate` returns `None`, not `0.0`, when nothing has completed. A rate of zero asserts the threshold was never reached — a finding; no data is not a finding. |
| 24 | §11 detection immutability | Enforced structurally: the model is `frozen=True` with `extra="forbid"`, so neither a mutation nor an added future-information field (`outcome_reached_5`) is possible without editing the model where review will see it. |
| 25 | §71 Discord authorization | Fails closed — an empty `AUREON_AUTHORIZED_USER_IDS` authorises **nobody**. Treating empty as "everyone" would turn a missing env var into an open trading bot. |
| 26 | §63 weekly review doc id | `{iso_year}-W{iso_week:02d}`, e.g. `2026-W03`, so ids sort chronologically as strings. |
| 27 | §21 threshold map keys | `threshold_key()` renders integral thresholds without a decimal point (`3`, not `3.0`), so `3` and `3.0` cannot become two buckets in the same `reached` map. |

---

## Decisions added while implementing Phase 2

`docs/ARCHITECTURE.md` is still absent, so §13's exact wording was unavailable and
the EMA-cross rule below is the standard reading. Rows 29 and 31 are the two most
worth checking against the real §13, because they change detection counts.

| # | Spec | Decision |
|---|------|----------|
| 28 | §9 fixture provenance | The committed `XAUUSD_M5.csv` is **synthetic** (seeded random walk via `scripts/gen_fixtures.py`), as no real broker export was available — the spec permits this if stated. It deliberately spans a weekend, so the 49h discontinuity exercises gap handling and Phase 3's `INVALID` horizons rather than leaving them untested. |
| 29 | §13 `sequence_today` / `sequence_session` | Count **candles** within the broker day and session, not detections. The engine must build a context before knowing whether any agent will emit, so a per-agent detection count is not available at that moment; and it is recoverable by querying `detections`, whereas a candle index is deterministic under replay. |
| 30 | §79/§82 engine window | `AnalysisEngine`'s window length is **fixed** and derived from `max(agent.min_window())`, identical in live and replay. This is a **correctness** parameter, not a tuning knob: EMA and Wilder's RSI are recursive, so a 30-bar and a 200-bar window disagree on EMA(21) by enough to move a crossover onto a different candle. `test_a_shorter_window_breaks_parity_as_expected` pins it. |
| 31 | §13 what counts as a cross | A cross is a **change in the sign of `fast − slow`, ignoring bars where they are exactly equal**. Comparing only against the previous bar is wrong either way: treating equality as the opposite side invents a cross for `1, 2, 2, 1` against `slow = 2` (a touch from below that fell back), while requiring strict inequality misses `3, 2, 1` (a real cross passing through equality). Carrying the last non-zero side forward handles both. |
| 32 | §75 backfill warm-up | The observer's backfill reaches back a full `window_size` **before** the cursor, feeds those candles to rebuild the engine's window, and discards their detections. Without this a restart silently produced detections that never existed and missed ones that did — caught by `test_observer_recovery` and pinned there. |
| 33 | §13/§14 RSI's role | RSI is attached as **context only** and never gates a cross. Keeping the gate out means the stored detections record what the crossover rule saw, so "would RSI have helped?" stays answerable from `detection_evaluations` instead of being baked into what was recorded. |
| 34 | §10 weekly schedule | The weekly open/close (Sun 22:00 → Fri 21:00, plus a 60-minute `PREOPEN`) is expressed in **UTC**, deliberately separate from §18's session windows, which are market-local. The weekly boundary is a market-wide convention, not a property of whichever broker clock we read. This is what makes Sunday 21:30 UTC (`PREOPEN`) differ from 23:30 UTC (`OPEN`). |
| 35 | §79 agent window shape | Agents receive a pandas `DataFrame` with a UTC `DatetimeIndex` of candle **open** times and columns `open, high, low, close, tick_volume, real_volume`, oldest first, last row = the candle just closed. `validate_window` rejects anything else so a malformed window fails with a clear message rather than as a wrong indicator. |
| 36 | §83 outbox durability | SQLite in WAL mode with `synchronous=FULL`. The table's entire purpose is surviving an abrupt kill, so trading a real durability guarantee for write throughput on a few detections an hour would be a bad bargain. `delivered_at` is set strictly **after** the remote write returns. |

---

## Decisions added while implementing Phase 2 Part B

`docs/ARCHITECTURE.md` is still absent, so §14–§18 were unavailable and every agent
rule below is the standard reading. Rows 40, 41 and 44 are the ones most worth
checking against the real sections, because they change what is detected.

| # | Spec | Decision |
|---|------|----------|
| 37 | §79 engine windows | `AnalysisEngine` holds one buffer sized to the hungriest agent but hands **each agent exactly its own `min_window()`**. Without this, window dependence leaks between agents: registering the liquidity agent (583 bars) beside the cross agent (64) would widen the shared window and silently rewrite the cross agent's history, invalidating the recorded baseline just by adding a sibling. `test_an_agent_is_unaffected_by_its_siblings` pins it. |
| 38 | §14 RSI agent | Emits on zone **transitions** (`overbought_entry` / `_exit`, `oversold_entry` / `_exit`), not while a zone holds. RSI sits above 70 for long stretches; emitting throughout would produce hundreds of near-identical detections and bury the bar where the state actually changed. Context-only: `direction` is always `None`. |
| 39 | §18 session trend | Context-only (`direction=None`), although a session that closed higher has an obvious "up" direction. A session summary describes the **past**, and Phase 7's inferred-link rule matches on "same symbol, same direction, trade opened shortly after" (§50) — a directional session detection would attract spurious links to unrelated trades and quietly corrupt the review. The trend is carried as a label plus the numeric change instead. |
| 40 | §15 liquidity sweep | `event_key` is `"{direction}\|{level_type}"` where *direction* is **where price went** (`up` through a high), while `Detection.direction` is the **implied reversal** (an up-sweep is a SELL bias). The two deliberately disagree; conflating them is the easy mistake. This is a strategy claim rather than an observation, so it wants confirming against §15. |
| 41 | §17 breakout | `Detection.direction` **agrees** with the break direction — a continuation, not a reversal — which is the mirror of row 40. A breakout also requires the **previous close on the other side of the level**, so price holding above a level reports one breakout rather than one per bar for the rest of the session. |
| 42 | §15/§17 level timing | Levels are computed as of the **previous** bar. Otherwise a candle can sweep or break a level it helped create, which is trivially true and meaningless. |
| 43 | §15 swing definition | A swing is a fractal pivot: strictly greater (or less) than `strength` bars **on each side**. Strictly, so a flat double top is not a fresh pivot but an already-tested level. A pivot is only reported once `strength` bars have closed after it — reporting an unconfirmed one would use future information, since the bar might yet be exceeded and the "level" vanish. |
| 44 | §15/§17/§16/§18 thresholds | Penetration, rejection, close-beyond, wick ratios and the session flat band are **placeholders**, tuned only to be non-degenerate. They are deliberately permissive rather than restrictive: detections are immutable, cheap and evaluated in Phase 3, so recording a marginal sweep costs little while missing one is unrecoverable. Expect to retune against §15/§17 and against Phase 3's reached-N results. |
| 45 | §18 who writes `sessions/` | The **observer** writes them, not the agent: an agent that touched Firestore would break both the purity contract and the rule that only repositories write (CLAUDE.md). They also bypass the outbox, because a session summary is derived entirely from candles the observer re-processes on restart — a lost write regenerates, so failing loudly would stop observation for something recoverable. |
| 46 | §18 internal day keys | `LevelTracker` groups bars into broker days using **opaque integers** (local-midnight epoch), never formatted date strings — they are only ever compared for equality. Measured over the same workload: per-element `strftime` 1.6s, `DatetimeIndex.strftime` 4.9s, integer keys 0.25s. `tz_localize(None)` is applied first so a DST transition day cannot make the flooring ambiguous. |

---

## Decisions added while implementing Phase 3

`docs/ARCHITECTURE.md` is still absent, so §21–§23 were unavailable. Row 47 is the
one to read first: it is a finding about the specified thresholds, not a preference.

| # | Spec | Decision |
|---|------|----------|
| 47 | §21 threshold scale | Thresholds are in **points**, consistent with every other distance in the system. At XAUUSD's `point = 0.01` the specified 3/5/10/15/20 are $0.03–$0.20 — far inside a single M5 candle's ~$0.45–1.10 range — so **every threshold is reached on the first candle, in every horizon, 100% of the time**. The rule is implemented exactly as specified and the saturation is reported as a finding rather than quietly retuned: the rule is frozen by design, and the mechanism for a better scale is a new `rule_id`. The report and the baseline both flag it automatically. **Confirm the intended unit against §21 before drawing any conclusion from these numbers.** |
| 48 | §22 evaluation scope | Only detections **with a direction** are evaluated. Without one there is no favourable side, so "did it work?" has no meaning. Context-only detections (`rsi`, `session_trend`, `wick`) are still stored and still available to the reviews as context; the backfill reports how many were skipped for this reason. |
| 49 | §23 same-candle crossings | When the favourable and adverse thresholds are first crossed **within one candle**, their order is unobservable from candle data. §23's wording ("MAE_FIRST if adverse happened *before* favourable; MFE_FIRST otherwise") puts these in MFE_FIRST — the optimistic side. Rather than hide that, a new `path_ambiguous` flag records it so a review can exclude them. On the fixture this is 463 of 470 classifications, which is itself the clearest evidence for row 47. |
| 50 | §22 where evaluation runs | **Inside the observer process**, which the spec offers as one of two options. It needs exactly the closed-candle stream the observer already has; a separate process would mean a second subscription that could drift out of step with the one producing the detections. Finished evaluations are released from memory so a long-running observer does not accumulate them. Splitting it into `main_evaluator.py` later needs no change to the tracker. |
| 51 | §23 excursion timing | A candle's high and low happened *somewhere inside* it, and candle data cannot say when. So the time a threshold was reached is recorded as that candle's **close** — the first moment the crossing is actually known. Using the open would claim knowledge that did not yet exist and make every `time_to` optimistic. |
| 52 | §22 durability | Evaluations are written straight to Firestore, bypassing the outbox. An evaluation is a pure function of its detection and the candles that followed, so a lost write is regenerated by re-running the backfill — unlike a detection, which can never be re-derived once its candle has left the provider's history. |
| 53 | §23 MFE sign | `mfe` is the furthest price ran in the detection's favour and **may be negative**, meaning the best price ever offered was still worse than the reference. That is informative, so it is stored raw rather than clamped to zero. `mae` is its mirror and is normally negative. |
| 54 | §21 `opposite_cross` | Completed by a later detection from the **same agent, symbol and timeframe** with the opposite direction. An opposite-direction detection from a *different* agent is a different opinion, not this agent reversing, so it does not end the horizon. |

---

## Decisions added while implementing Phase 4 (the money phase)

`docs/ARCHITECTURE.md` was still absent, so **§41, §56 and §57 — the guard's bullet
list — could not be read**. Row 55 is the most important row in this document: it states
what the guard checks and why each rule is there, entirely as inference. A guard missing
a bullet is a money-losing bug, and only the frozen spec can say whether one is missing.

| # | Spec | Decision |
|---|------|----------|
| 55 | §41/§56/§57 guard rules | Seventeen rules, each its own function with its own test: `trading_enabled`, `symbol_allowed`, `confirmation_fresh`, `market_open`, `symbol_available`, `quote_available`, `quote_fresh`, `spread_within_limit`, `price_has_not_run_away`, `volume_valid`, `volume_within_max_lot`, `filling_mode_supported`, `stops_valid`, `pending_entry_valid`, `margin_sufficient`, `within_open_position_limit`, `within_daily_trade_limit`. Every "cannot tell" answer is a **refusal**, not a pass. **Reconcile this list against the real §56/§57/§41 before trading a funded account.** |
| 56 | §41 deviation | The guard **clamps** the requested deviation to `max_deviation_points` and never widens it. A request asking for more slippage tolerance than policy allows gets policy's. |
| 57 | §41 price drift | `price_has_not_run_away` compares the execution-time price against the **confirmed** quote and refuses only **adverse** movement beyond `max_deviation_points`. Refusing a fill that improved on what the human saw would be perverse. Distinct from the spread rule, which measures the book's width rather than its movement. |
| 58 | §30 unmapped broker retcodes | An unrecognised MT5 retcode is treated as a **rejection**, not an unknown outcome: the broker returned a code, so it did decide. Only the *absence* of an answer (a raised exception, a `None` result) leaves the request `EXECUTING`. Getting this backwards would either strand good requests or invite a re-send. |
| 59 | §35 reconciliation matching | Matched on magic + comment token + symbol + direction, with volume as an **upper bound within tolerance** rather than an equality — a partial fill is a legitimate match with a smaller volume, and insisting on equality would make every partial fill unfindable. Deals sharing a `position_id` are collapsed into one candidate, so a multi-deal fill does not look like multiple orders. |
| 60 | §35 ambiguity | More than one matching broker record → `FAILED_RECONCILIATION` with the candidates listed, never a guess. Two matching orders means something already went wrong, and picking one could attach the request to the wrong position. |
| 61 | §35 a vanished pending order | A `PENDING` order that is no longer at the broker and never filled resolves to **`CANCELLED`**, not `EXPIRED`. Claiming expiry would assert an expiry time we never observed. |
| 62 | §29 the stale-claim rollback | `claim` writes `FAILED_STALE` and **returns**; the refusal is raised by the caller *after* the commit. Raising inside the transaction aborts it and rolls the write back, leaving the request `CONFIRMED` and claimable later at a price the human never saw. This was a real bug, caught by scenario D. |
| 63 | §32 same-status resolves | `resolve` short-circuits only when there is genuinely **nothing to write**. An earlier version returned early on any unchanged status, silently discarding the `comment_token` the executor stamps before sending — leaving reconciliation nothing to search for. Also a real bug, caught by scenario A. |
| 64 | §87 test substrate | The failure-injection suite runs against the **real Firestore emulator**, via `make emulator` (Node + Java, no Docker) or `docker-compose.emulator.yml`. Exactly-once rests entirely on Firestore's transaction semantics, so an in-memory double would be testing my model of those semantics rather than the semantics. Without `FIRESTORE_EMULATOR_HOST` the suite **skips loudly** rather than passing vacuously. |
| 65 | §87 emulator behind a proxy | gRPC honours its own proxy variables, so a session with `HTTP(S)_PROXY` set cannot reach a localhost emulator (`Expected SETTINGS frame as the first frame`). `no_grpc_proxy=127.0.0.1,localhost` is the targeted bypass; the Makefile sets it. This disables nothing else. |
| 66 | §56 settings failure | `ExecutionSettingsRepository.read_or_default` returns `ExecutionSettings()` — i.e. `trading_enabled=False` — when the document is missing or unreadable. An absent settings document must not become an open trading bot. |

---

## Decisions added while implementing Phase 5

`docs/ARCHITECTURE.md` was still absent, so §36, §44, §45 and §52 are inferred.

| # | Spec | Decision |
|---|------|----------|
| 67 | §49 trade document id | `{account_scope}__{mt5_position_id}`, deterministic. The monitor re-sees every position on every poll and after every restart; a random id would accumulate one document per sighting. `account_scope` is included because two accounts can legitimately hold the same position id. |
| 68 | §44 realized P&L | The **sum of the deals' profit**, plus commission and swap, exactly as the broker reports them — never recomputed from prices and volumes. A recomputation would silently disagree with the account statement the moment a contract size, a currency conversion, or a two-price partial close is involved. The broker's number is the one the money followed. |
| 69 | §36 exit matching | Exits are matched on `mt5_position_id` + deal `entry` type, **never on the comment**. A closing deal often carries no comment, or one the broker wrote itself (`"sl 2398.00"`); matching exits by comment would miss every stop-loss and every close from a phone. The comment is still used for *entries* (Phase 4), where it is the only way to find an order whose result was lost. |
| 70 | §44 close price and reason | Taken from the **last** exit deal by time — the price the position finally left the market at. Deals are sorted before folding, because broker history can arrive out of order. |
| 71 | §44 incomplete exit evidence | A position gone from the broker's open list whose visible deals do **not** account for its full volume is recorded `PARTIALLY_CLOSED`, not `CLOSED`. `CLOSED` is terminal, so closing on partial evidence freezes a wrong `realized_pnl` that no later poll can correct. Caught by a test; a broker timestamp one second ahead of ours is enough to trigger it. |
| 72 | §44 deal window | The deal query reaches back `DEAL_OVERLAP_SECONDS` before the last sync **and 60s forward** of now. Backward because a deal can be published slightly out of order; forward because the broker's clock is not ours and a deal stamped ahead of us would otherwise be invisible for a whole poll — long enough to see only part of a multi-deal exit. Re-reading is free: every write here is idempotent. |
| 73 | §45 excursion units and side | Points, signed by the position's direction, so a BUY and a SELL are directly comparable — the same convention the detection evaluator uses. Measured at the **exit** side of the book (bid for a long, ask for a short), because that is the price the position could actually be closed at; the mid would flatter every excursion by half the spread. |
| 74 | §45 the weaker label wins | A position resuming live tracking over **reconstructed** figures keeps the `reconstructed` label. The record must describe the weakest measurement it contains, not the most recent one. Reconstruction also ignores candles outside the position's life, or someone else's price action gets attributed to this trade. |
| 75 | §45 first observation | The first observed price sets **both** extremes, so `mae` can legitimately be positive early on ("the worst it got was still in our favour") for the same reason `mfe` can be negative. Both are stored raw rather than clamped. |
| 76 | §52 what counts as external | No Aureon magic, **or** Aureon magic with no matching request → `source=external_mt5`, `trade_request_id=None`. The second case matters: our magic without a request means we cannot account for it, so it is observed and never managed. |
| 77 | §43 pending order resolution order | A **fill is checked first, always** — before expiry, before cancellation. An order can fill in the same instant a cancel is issued (Phase 4 scenario G), and recording `CANCELLED` for a position that actually exists would leave a live trade nobody is watching. That is the worst of the three mistakes by a wide margin. |
| 78 | §58 monitor independence | The monitor polls regardless of `trading_enabled`. That switch stops the executor; positions opened before it was thrown are still live and still need their closes recorded. A monitor that paused alongside the executor would blind the operator during exactly the incident that made them disable trading. A test asserts the monitor makes no order-placing broker call at all. |

---

## Decisions added while implementing Phase 6

`docs/ARCHITECTURE.md` was still absent, so §37–§47, §57 and §71 are inferred. Rows 79
and 80 resolve a genuine contradiction in the constraints rather than an ambiguity.

| # | Spec | Decision |
|---|------|----------|
| 79 | §39/§42 vs CLAUDE.md | §39 requires Discord to build the execution-mode selector from `symbol_info.filling_modes`, and §42 requires it to validate a lot against the broker's min/max/step — but **Discord may never call the broker**. Resolved by having the observer publish `SymbolInfo` to `symbol_specs/{symbol}`, which Discord reads like everything else. The copy is a **convenience, not an authority**: it can be stale, so Discord's validation is advisory and the execution guard re-reads the live symbol (§56). A *missing* copy therefore does not block a trade. |
| 80 | §40/§27 vs "tick data never goes to Firestore" | §40 requires bid/ask on the confirmation screen and §27 requires a quote-age check, but Discord cannot call the broker and tick data may not go to Firestore. Resolved with `SymbolState.last_quote`: **one current snapshot per symbol, overwritten in place** and throttled with the rest of `system_state`. That is state, not a tick stream — the prohibition is about volume and append-only history, and this is neither. When no quote has been published, `check_confirm_press` treats it as "cannot confirm, refresh" rather than as permission to proceed. |
| 81 | §37-§57 layering | All Discord logic lives in `aureon/discord/service.py`, which **imports no discord.py**. The rules that protect real money — who may confirm, whether a quote is too old, whether a lot is legal — are therefore tested directly, without a gateway, a guild or an event loop. `bot.py`, `commands/` and `views/` are thin adapters that render the results. |
| 82 | §71 async discipline | Firestore's Python client is synchronous, so every call from a handler goes through `BotContext.run` → `asyncio.to_thread`. Not an optimisation: discord.py must acknowledge an interaction within three seconds, and a synchronous round trip on the event loop can miss that and lose the interaction entirely. Every handler `defer()`s first for the same reason. |
| 83 | §27 stale quote behaviour | A stale quote **re-prompts**, it does not refuse. The human still wants the trade; they need current numbers. A refusal would make them restart the wizard for a reason that is not about their intent. |
| 84 | §57 switch asymmetry | Disabling trading is **immediate, with no confirmation step**; enabling requires a confirming press. Disabling is the safe direction and the one reached for during an incident, so it must not have a speed bump. Enabling arms real money and deserves the pause. |
| 85 | §46/§47 control requests | Discord writes a `control_requests` document and stops — it does not call the broker, and it does **not** report the outcome from its own return. The executor performs it under the same claim/lease discipline as a trade, and the monitor records what happened. A cancel can lose a race to a fill and a close to a stop-loss; reporting "cancelled" optimistically is how a human comes to believe an order is gone when it is a live position. |
| 86 | §71 startup validation | `main_discord.py` refuses to start without a token **or** without a non-empty allowlist. An empty allowlist authorises nobody, so the bot would answer no one while appearing healthy — worse than a clear failure. A missing guild id is only a warning, but a loud one: a global command would appear in every server the app joins, and this one places trades. |
| 87 | §8 result reporting | A request's outcome is reported by a Firestore listener on that document, never from the CONFIRM button's return. **Every terminal status renders, including the failures**, with the reason the executor recorded. A human who authorised real money and heard nothing back has been failed worse than one who was told it was rejected. |

---

## Decisions added while implementing Phase 7

`docs/ARCHITECTURE.md` remains absent, so §50, §61–§65 are inferred. Row 90 is the one to
re-read if the reviews ever look too flattering.

| # | Spec | Decision |
|---|------|----------|
| 88 | §61-§63 scheduling | Reviews run as a **separate `main_review.py` process**, not a job inside the observer. A review is batch aggregation over history: it needs no live feed, and coupling a minutes-long read of thousands of documents to the loop that must not miss a candle close would trade a real-time guarantee for a reporting one. It must also be re-runnable for any past period — to backfill, or to regenerate after a bug fix — which a CLI makes one command. Reviews are idempotent, so running one twice is harmless. Scheduling is left to cron or a systemd timer; the command defaults to the most recently *completed* period, so "run it after close" is the whole configuration. |
| 89 | §63 period boundaries | A review covers a **broker** day or ISO week, as a half-open `[start, end)` window in UTC derived from market-local midnight. Half-open so consecutive periods tile without double-counting the boundary instant, and market-local because 23:30 UTC is already tomorrow in Athens — a UTC-bounded review would file a session under the wrong day and every per-day figure would be subtly wrong with nothing in the output revealing it. ISO weeks rather than a broker convention, because ISO is unambiguous across year boundaries. An emulator test pins the exact boundary: 21:00 UTC belongs to the next broker day, 20:59 to the previous one. |
| 90 | §61 reached counts | The reviews package may read **only** `complete_horizons`, `pending_horizons` and `invalid_horizons` — never `.horizons` directly. An AST-precise boundary test enforces it. Folding an unknown outcome into a denominator is the single easiest way to make a strategy look worse than it is, and the resulting figure ("3 of 10 reached") is indistinguishable from an honest one. `pending_horizons_excluded` and `invalid_horizons_excluded` are reported on every review so a reader can tell "3 of 10" from "3 of 10, 40 still unknown". |
| 91 | §50 inferred links | An inferred detection→trade link lives **only on the review**, never on the trade, and `InferredLink` requires a `confidence`. Reviews therefore write only their own documents: analysis must not edit the record it analyses. A link is inferred only when **all four** hold — same symbol, same direction, trade opened strictly after and within `AUREON_INFER_WINDOW_MINUTES`, same session — and at most one detection is linked per trade. `confidence` is a rank for ordering candidates, not a probability; nothing may treat it as one. An explicit link is left exactly as the human stated it. |
| 92 | §64 classification | Execution vs observation uses six values, and `UNKNOWN` is one of them: a detection whose horizon is still PENDING or INVALID classifies as `UNKNOWN`, never as `MISSED_AND_NOT_REACHED`. A trade with no link at all is `DISCRETIONARY` rather than an error — humans trade for reasons the machine never saw. The counts are rendered in `notes` with every value named including the zeros, because a reader who cannot see how much was unresolved cannot judge the rest. |
| 93 | §61 review determinism | Regenerating a period **overwrites the same document id** and must produce byte-identical content: a period has one answer, and if the answer changes the old one was wrong rather than also true. Every collection is sorted before folding and no wall-clock value enters the result except `generated_at`, which the caller supplies. Tests assert byte-identity across re-runs and stability under input reordering. |
| 94 | §61 capability split | `/status` holds `ReviewReader`, which has **no write method**, not `ReviewRepository`. A human interface able to rewrite a review would make every figure built on those reviews unfalsifiable. The split is visible in the import graph and enforced by a boundary test, rather than resting on nobody calling the wrong method. This was written because the boundary test caught the repository being wired into the Discord context. |
| 95 | Phase 7 gate | `docs/PHASE2_BASELINE.md` carries a **Weekly review reconciliation** section, and `scripts/gen_baseline.py` refuses to write the document when it does not reconcile — a stale baseline is recoverable, one asserting counts that disagree is worse than none. The two paths are genuinely independent: the baseline counts with a `Counter` over the engine's output, the review with market-clock week windows and COMPLETE-only reached counts. Verified load-bearing by reverting: bounding the week in UTC moves one detection and is caught; folding PENDING into the denominator is caught on four horizons. |
| 96 | §63 weekly default | The weekly review's default period is the **most recently finished trading week**, not "seven days ago": take the most recently completed broker day, walk back to the most recent Friday at or before it, and take that Friday's ISO week. §63 asks for the review to be generated after Friday's close, and subtracting seven days names the week *before* the one that just traded on Friday, Saturday and Sunday — so the cron the spec describes would produce last week's numbers under this week's heading, with nothing in the document revealing the substitution. Found by a test of the CLI's defaults; every weekday is now pinned, plus a year-long invariant that the default never names a week still trading. |

---

## Decisions added in the corrections slice (before real MT5 data)

`docs/ARCHITECTURE.md` is **still absent** from the repository, so §11–§13, §21–§23,
§56–§60 and §66 could not be read. The contracts below come from the task description,
which stated them explicitly; every § citation remains an unverified reference.

| # | Spec | Decision |
|---|------|----------|
| 97 | §12 detection id | `detection_id` = sha256 over `account_scope`, `symbol`, `timeframe`, `candle_close_utc`, `agent_name`, `agent_version`, `event_key`, joined by `0x1F`, in that order. This **reverses decision 16**, which excluded `agent_version`. The consequence is deliberate and must be understood before bumping any agent: a version bump now *forks* history rather than upserting it — the new version's detections sit beside the old version's for the same candles. That is what lets `ema_cross` 1.0.0 (9/21) and 2.0.0 (20/50) describe the same week side by side, and it is also why a version bump must never be used to correct history in place. The timestamp also moved from the candle **open** to the candle **close**, the instant the detection became knowable. `DETECTION_ID_COMPONENTS` names the tuple so a test pins the contract rather than a hash, and `docs/CONTRACTS.md` now generates the component table. |
| 98 | §12 migration | `scripts/rekey_detections.py` copies mis-keyed detections to their §12 id; it does **not** move them unless `--delete-old` is passed, and it refuses to write outside the emulator without an explicit acknowledgement. It is idempotent: a second run reports the copies as `unchanged` and issues no writes. **It cannot split a collision the old recipe already caused** — because the old id omitted `agent_version`, two versions of one agent observing the same candle wrote to the same document and the second overwrote the first. Only one survived. The 9/21 history therefore comes from replaying with that agent, not from this script. |
| 99 | §13 EMA periods | `AUREON_EMA_FAST` (20) and `AUREON_EMA_SLOW` (50). `EmaCrossAgent` takes **no default periods at all** — both are required keyword arguments. A default is the specific bug the signature prevents: the observer would read 20/50 from config while a replay, script or test silently constructed 9/21, and the two would disagree about which candles produced a cross while both looked correct. Added to `aureon/config/config.py` rather than a new `aureon/config/settings.py`, so configuration stays in one module (`aureon/models/settings.py` already exists and holds the Firestore runtime gates — a third settings location would be worse than the naming mismatch). |
| 100 | §13 cross numbering | On a cross detection, `sequence_today` / `sequence_session` are the **cross ordinal** (1st, 2nd, 3rd of the day and of the session), not the candle index. Every other agent keeps the candle index, where the index *is* the meaningful ordinal. Numbering happens in the **engine**, not the agent: an agent is a pure function of `(window, ctx)`, counting across candles is state, and state in an agent would break the replay/live parity contract. §12 excludes the sequence, so re-numbering cannot re-key a detection. |
| 101 | §66 cross counters | Six counters on `SymbolState`, reset on the **market** clock: `ema_crosses_today/session`, `bullish_crosses_today/session`, `bearish_crosses_today/session`. Six rather than two because "4 crosses today" and "3 up, 1 down" answer different questions and neither is derivable from the other. A day reset clears the session tallies too (a day contains its sessions); a session change leaves the day tallies alone. The observer reads them from the engine rather than recounting, so a second tally cannot drift from the first. |
| 102 | §21 threshold units | `EvaluationRule.threshold_unit` is `points` or `price`. `EMA_OUTCOME_V1` stays `points` and is **frozen** — a test pins its `definition_hash`, so editing its thresholds fails with the rule named rather than silently redefining every result already stored under `{detection_id}__EMA_OUTCOME_V1`. `XAU_OUTCOME_V2` uses the same horizons and reference price with thresholds `[3, 5, 10, 15, 20]` in **price**. The points equivalent is **derived from `symbol_info.point` at evaluation time** (`thresholds_in_points`), never a hard-coded multiplier, so the rule means "$5" on any symbol. V1's 3–20 points at `point=0.01` is $0.03–$0.20, inside a single XAUUSD M5 candle — which is why every V1 threshold read as ~100% reached and measured the tick size rather than the strategy. |
| 103 | §21 default rule | `AUREON_EVAL_RULE` defaults to `XAU_OUTCOME_V2`. The older `AUREON_EVALUATION_RULE_ID` still wins when set, so an existing `.env` pinning V1 keeps getting V1 rather than silently switching to a rule with different thresholds — for a variable that decides which numbers you get, the safe direction is to honour the explicit pin. |
| 104 | §75 restart reach-back (**bug fixed**) | The observer's backfill reaches back a window's worth of **candles**, not of minutes. `_reach_back` widens its request until it actually yields `window_size` candles at or before the cursor. The old code computed `cursor - window_size × timeframe`, which is only equivalent while the market never closes: restarting in the ~151 candles after the fixture's 49-hour weekend gap, the request landed *inside* the gap and returned 72 of the 151 needed, so the fresh engine's EMAs never converged and the restart **silently lost a detection the uninterrupted run produced**. This was a latent bug all along — the 9/21 pair survived it by coincidence, needing only 64 bars of reach, which happened to fit in the six hours of candles after the gap. It surfaced the moment the warm-up grew to 150. Kill points 1429 and 1500 in `test_restart_produces_no_duplicates_and_no_gap` pin it; reverting the fix fails both. The widening deliberately does **not** stop when a doubling returns no new candles, because "asked for earlier candles and got none" is indistinguishable from the start of history — an early exit on the first unchanged count is exactly what left the reach-back starved. |
| 105 | §19, §23 context tags | Four tags on each evaluation — `has_lower_rejection_wick`, `has_upper_rejection_wick`, `sweep_same_direction_within_N_candles`, `session_trend_aligned` — derived **only** from detections at or before the subject's own candle close, within `AUREON_CONTEXT_WINDOW_CANDLES` (default 3). This module is the easiest place in the system to leak the future: "look at the detections around this one" has no natural sense of direction in time, and a wick forming on the *next* candle would tag the cross retroactively, manufacturing a correlation that looks real and cannot be falsified. One line in `window_for` enforces it and four tests catch a symmetric window. A `flat` session trend is never alignment — calling it aligned would put every flat session in the "aligned" bucket for both directions. Tags live on the evaluation, never on the detection, because a detection is immutable and must carry no information about its surroundings that was not in its own candle. |
| 106 | §23 tag comparison | The review reports each tag as **both** sides — `with` and `without` — from COMPLETE horizons only. A "with" rate alone is unreadable: 60% means nothing until the "without" number is known. A comparison whose either side has fewer than 5 COMPLETE horizons is labelled "too few to read" rather than printed as a percentage, because at those counts one detection moves the rate by twenty points. The comparison is printed by `main_review.py`, not stored on the review document: it is explicitly research, and a week of 29 crosses cannot support a claim either way. An evaluation with no tags at all counts as `without` rather than being dropped, so an older document cannot silently shrink the denominator. |
| 107 | §57, §60 kill switch | `set_trading_enabled` writes the settings document **and** its `AuditRecord` in ONE Firestore transaction, with a `settings_version` read-then-conditional-write. Previously it was read-modify-write plus a separate audit write: two concurrent toggles were last-write-wins on the one setting that decides whether real money can move, and the switch could flip with no audit row (or leave a row for a write that failed, which is worse — it claims something happened that did not). `if_version` lets a caller assert what it believed it was changing, for a UI that read the settings, rendered a button and got a press seconds later. The audit id is **deterministic** per version, so a transaction Firestore retries writes the same row rather than claiming two decisions where a human made one. |
| 108 | §58 terminal immutability | A terminal request (`FILLED`/`CANCELLED`/`EXPIRED`/`FAILED*`) or trade (`CLOSED`) refuses any field write except `last_reconciled_at` / `last_synced_at`. Both are observational: they record when something was last looked at and assert nothing about what happened. The guard covers the gap the transition table cannot see — a **same-status** write carrying `updates`, which could rewrite a settled trade's `realized_pnl` and make every review built on it unfalsifiable. Ordering matters: the transition table is consulted first for an actual status change, so `CLOSED → OPEN` still reports itself as an illegal edge rather than as a terminal write. The `EXECUTING → EXECUTING` path that stamps `comment_token` is explicitly preserved and tested — breaking it would trade one silent failure for a much worse one. Terminal sets are derived from the transition tables, so adding a terminal state protects it automatically. |
| 109 | CLAUDE.md repository boundary | Every raw Firestore read moved into a repository method, and two AST boundary guards replace the old import-only check: nothing outside `aureon/storage` may call `.collection(`, `.document(` or `.transaction(`, and nothing may reach into another object's `_client`. Six call sites existed — four in the review service, one in the Discord listener, one in the executor reaching into `repository._client` behind a `noqa`. All six were reads, which is exactly why this mattered: the rule meant to prevent a client-side money write did not constrain that shape of code at all, so the next raw call could have been a `.set()` and the suite would have stayed green. Resolving it exposed a genuine conflict between two CLAUDE.md rules — routing reads through repositories forces reviews to import write-capable classes, which §50 forbids — settled by `PeriodReader`, a read-only composition living inside `aureon/storage` with no write method, the same pattern and the same reasoning as `ReviewReader`. |
| 110 | §46, §47 control leases | `control_requests.resolve` verifies lease ownership exactly as trade requests do, with `reconciliation=True` as the same sanctioned exception. This was an asymmetry, not a decision: without it an executor whose lease had expired could still record the outcome of a cancel it no longer owned, and since a cancel can lose a race to a fill, a stale `completed` landing on top of the real `already filled` is how a human comes to believe an order is gone when it is a live position. `ControlWorker` also stopped passing the *claim* time to `resolve`, which would have let an operation that outran its lease pass the check it exists for. The missing two-executor scenario test is now present: one cancel, two workers, exactly one broker cancel. |
| 111 | §83 collection prefix | Every collection is `{AUREON_COLLECTION_PREFIX}_{name}`, default `aureon_beast`. `paths.collection()` is the only place a name is built, the constants keep their names and hold prefixed values (so every existing caller is already correct), and a boundary test forbids a bare collection literal anywhere under `aureon/` outside `paths.py`. A bare literal is the worst kind of bug here because it does not fail: reading `"detections"` while everything else uses `"aureon_beast_detections"` silently touches a second, empty collection, which looks exactly like "no data yet". The prefix is read from the environment **at import time** in one module rather than threaded through the repositories — a prefix that differed between two repositories in one process would split the database in half with both halves reporting success. An empty or all-underscore value falls back to the default rather than producing `_detections`. `tests/conftest.py` sets `aureon_test` **before any aureon import** (later would be too late, since `paths` freezes the value) and refuses to run on the production default without an emulator host. The emulator rules already match `{document=**}`, so they need no change; the real client-facing rules are still Phase 8. |
| 112 | §59, §66 market snapshot | `SymbolState` carries the full live picture: `ema_fast/slow/distance`, `rsi`, `rsi_zone`, `session`, `session_trend`, `session_high/low`, the six cross counters, `last_cross {direction, at, price, detection_id}`, `last_cross_at`, `last_sweep`, `last_wick`, `last_breakout`, `detections_today`. Written on every candle close; the quote and heartbeat fields stay on the existing throttle. `MarketSnapshot` assembles it **from the detections the engine already produced**, reading each detection's own `IndicatorSnapshot` rather than recomputing anything — a second implementation would, the first time the two disagreed, make the status screen quietly wrong about a number a human was using to decide something. The consequence is deliberate: a field stays `None` until an agent has emitted a detection carrying it, so a fresh start reports "unknown" rather than guessing. Session extremes come from candles, not detections, because a session has a high whether or not anything detected anything. A new broker day clears `detections_today` and the session extremes but **keeps the EMAs** — an EMA does not reset at midnight, and blanking it would make the screen claim no information on the first candle of every day. `observe` is idempotent per detection id, because the restart backfill replays candles it already processed. |
| 113 | §59 two status modes | `/status` is live **or** review, never both. On an OPEN market it renders the §59 panels; on a CLOSED market those numbers are a snapshot of whenever the market shut, and rendering them in a live layout invites reading them as current — so the closed screen shows the completed review instead, which is what a reader wants after the close. A missing value renders as an em dash, never `0.00`: a zero is a measurement and a dash is the absence of one, and on a line reporting "how far apart are the EMAs" a zero claims they have converged. The panel keeps its shape when values are missing, so it does not change layout as data arrives. `ema_distance` and `rsi_zone` are stored so a dashboard need not re-implement "where does overbought begin", but they are **derived in a model validator** whenever absent, so a document carrying an EMA pair always carries the matching distance and the two cannot drift apart. |
| 114 | §82 live-candle archive | The observer writes every closed candle it processed to `data/live_candles/{symbol}_{tf}_{market_date}.parquet`. The replay/live parity test proves the engine is deterministic **given the same candles**; it cannot prove the live path was given the same candles, because the live input only ever existed in memory. Without the archive, a live session producing different detections from a replay leaves no way to tell whether the engine drifted or the broker served different bars — and those two have completely different fixes. Parquet rather than CSV because a CSV round trip turns a float into a string and back, which is precisely where a comparison acquires a difference that was never in the data. Filed by **broker** date: a 22:00 UTC candle belongs to the next trading day in Athens, and a UTC-named file would split one session across two so a comparison for "Monday" would silently miss its first three hours. Buffered and flushed on day rollover and shutdown, merging with whatever is on disk — a restart mid-day that truncated the morning would make every missing morning detection read as a live/replay mismatch. A closed candle is not tick data: it is the same bounded, final record the detections are derived from. |
| 115 | §82 comparison | `scripts/compare_live_vs_replay.py` replays the **archive**, never a fresh broker fetch — a refetch would let "the broker served different candles the second time" masquerade as an engine difference. It reports three distinct kinds because they have different causes: *missing* (replay produced it, live never stored it — look at the outbox, then the window), *extra* (stored but not reproduced — usually a leftover from an earlier `agent_version`, which is exactly what §12 is for), and *mismatched* (same id, different content — the most serious, since the id is a pure function of the candle). `schema_version` is excluded from the content diff: it is metadata about the document, not about the observation, and including it would make a schema bump report thousands of mismatches and bury a real difference. Exits non-zero on any difference so it can gate a session review. The manual leg is `docs/MT5_SESSION_CHECKLIST.md`; `docs/PHASES.md` carries an explicitly **empty** "Phase 2 real-session evidence" section, because a section that did not exist would read as an oversight where an empty one reads as a gap. |
| 116 | §83 guard scope (**bug found**) | The bare-collection-literal guard scans `tests/` as well as `aureon/`, exempting only `paths.py`, that module's own test, and the guard itself. It was extended after the prefix landed and immediately found a real one: `stored_ids` in `test_observer_recovery.py` filtered on `"detections/"`, which matched nothing once collections were prefixed. The negative assertions then passed **vacuously** — empty list versus empty list — and only a single positive assertion failed. That is why a stale literal in a test helper is worse than one in production code: most of the test keeps passing and says nothing. |
| 117 | §83 generated docs vs the prefix | `docs/CONTRACTS.md` renders collections with the **default** prefix regardless of the environment. It has to: the test suite runs under `aureon_test`, so a generator that emitted the resolved prefix produced a document that could never satisfy `--check` in both a test run and production. The document describes the shape of the database; which prefix a given deployment uses is a deployment fact, and the file says so in a line above the table. |
| 118 | §22 evaluation coverage (D-4) | The outcome tracker filters on **direction, not agent name**, and always has — it has no allowlist. Verified empirically against the fixture: `liquidity` 224/224, `breakout` 111/111 and `ema_cross` 12/12 evaluated, `wick`/`rsi`/`session_trend` zero. D-4's premise that only `ema_cross` gets outcomes is false, so no code changed; what was missing was the **proof**. An allowlist is the wrong shape for this question: "does this work?" is about any detection with a favourable side, and an allowlist naming today's three agents would silently stop evaluating the fourth — a gap nothing reveals, because a sweep with no evaluation simply does not appear in a reached-N table, so the table still looks complete. Two tests now pin it: a whole-roster fixture replay, and a directional detection from an invented agent name (which the fixture test alone would not catch, since an allowlist naming exactly the three agents it uses would keep it green). |
| 119 | §59 status provenance (D-7) | `/status` reports **when** `system_state` was last written, not just LIVE/STALE/OFFLINE: `last updated 09:59:48Z (12s ago) — LIVE`, in the embed footer. Both the timestamp and the age, because they answer different questions — the age tells a reader whether to trust the numbers, the timestamp is what they quote when something looks wrong. A freshness word alone hides how far past the threshold a STALE screen has drifted: 46 seconds and six hours read identically. A missing `system_state` renders "never" rather than a zero or a blank, both of which would read as "just now". The rest of D-7's §59 layout was already present from decision 113; this was the one label missing, and a test now asserts the whole list rather than most of it. |
| 120 | §15-§17 placeholder thresholds (D-15) | **Recorded, not changed.** `min_penetration_points=5`, `min_rejection_fraction=0.25`, `min_close_beyond_points=10`, `min_wick_range_ratio=0.55`, `min_range_points=20`, `flat_points=50` are **v1.0.0 placeholders chosen to be plausible on gold and researched on nothing.** No live session, no backtest, no outcome study stands behind any of them. Every count in `docs/PHASE2_BASELINE.md` for `liquidity`, `breakout`, `wick` and `session_trend` is therefore a count of "events these arbitrary numbers happened to select", and must not be read as a measurement of the market. Changing any of them is a **new `agent_version`**, not a preference: the values live in `agent_params_snapshot` and the version is part of the `detection_id` (decision 97), so a retune without a bump leaves the old detections at ids the new agent would never produce, indistinguishable from the new ones. D-1 is what makes retuning safe to do and safe to undo. `aureon/config/symbol_tuning.py` adds the per-symbol hook now, before a second symbol exists, because these thresholds are **not dimensionless**: `5 points` is $0.05 on XAUUSD and means something entirely different on a symbol with a different tick and daily range — running XAGUSD on gold's numbers would produce detections that look like sweeps and are noise, with nothing downstream to say so. XAUUSD's values are byte-identical after the change, pinned by a test. |
| 121 | §22, §23 outcome reporting (P-1) | `XAU_OUTCOME_V2` is reported per **agent** and per **session** and per horizon, not as one table per horizon. A single table averages a London breakout with an Asia sweep and reports the average as if it described both — and on this fixture those groups are genuinely different: `breakout` reaches $5 by `day_close` in 80% of Asia's COMPLETE horizons and 44% of New York's. `aureon/evaluation/outcome_report.py` is the one aggregation, shared by `docs/PHASE2_BASELINE.md` and `scripts/report_outcomes.py`, so the document and the operator's terminal cannot disagree about a figure. Medians are rendered in the **rule's own unit** (price for V2), converted from the stored points with `symbol_info.point` — printing 700 under a heading of `$3 / $5` is how a reader concludes gold moved 700 dollars. `median t→$5` is taken over the horizons that **reached** $5, with that count printed beside it, because a non-reach has no duration; treating one as zero or infinite produces a number that is not a time. `0/0` renders as an em dash, never `0%`: a zero is a measurement. The V1 table is kept and regenerated but labelled **HISTORICAL — invalid for research**, since a rule is frozen once shipped (§21) and V1's numbers are still what the stored V1 documents mean — they just measure the tick size rather than the strategy. |
| 122 | §63 reconciliation rule (P-1) | The baseline's weekly-review reconciliation runs on **`XAU_OUTCOME_V2`**, not V1. `AUREON_EVAL_RULE` already defaults to V2, so the reviews the running system generates were being cross-checked against a rule nothing evaluates any more — and worse, the check agreed trivially, because every V1 reached count is saturated at 100% and two paths both counting "all of them" cannot disagree. Under V2 the same check compares varied numbers (3, 10, 18, 12, 17, 14, 18 at $3 across the seven horizons), so a week-boundary or PENDING-folding bug now has something to be wrong about. The generator does ONE whole-roster V2 replay and slices it to `ema_cross` for the reconciliation rather than replaying twice: the engine gives each agent its own window slice and a detection's horizons are evaluated independently of other agents' detections, and `tests/unit/test_baseline_reconciliation.py` re-derives the same figures from a **cross-only** V2 replay against the committed document, which is what turns that reasoning into a check. |
| 123 | §67, §82 preflight (P-2) | `scripts/preflight.py` verifies, against the real things, everything a session depends on: config identity and the market zone's **current** offset, the collection prefix, the outbox and archive directories, a Firestore write-and-read-back of `{prefix}_heartbeats/preflight`, `trading_enabled`, MT5 init, the logged-in login/server against the config, the symbol's tradability and specs, a `symbol_specs` publish-and-verify, and clock drift. Five statuses, not two, because "checked and wrong", "not what you probably meant" and **"not checked"** are different facts: a SKIP never reads as a pass, the summary line names how many checks did not run, and only a FAIL makes the exit non-zero. `trading_enabled` is INFO — whether it should be on depends on which session this is (an observation run wants false, a demo drill wants true), so preflight refuses to have an opinion and refuses to let it go unseen. It writes exactly two documents, both of which the observer would write seconds later: `heartbeats/preflight` (deliberately NOT one of `paths.SERVICES` — writing `heartbeats/observer` would make a dead observer look alive) and `symbol_specs/{symbol}`. |
| 124 | §8 clock drift is asymmetric (P-2) | Drift is measured from the broker's last tick, and the two directions are **not** symmetric. A tick timestamped in the future is unambiguous — no tick can have happened after now — so it is a FAIL. A tick in the past by more than the tolerance is **inconclusive**: a quiet market and a fast local clock produce the identical number from one sample, so it is a WARN naming the lag, and past `MARKET_IDLE_SECONDS` it is a SKIP saying the market looks closed and nothing was measured. Reporting either as a pass would be the more dangerous error, because local clock drift is exactly what corrupts candle boundaries: the boundary is decided on the LOCAL clock, so a clock running two seconds fast reads a bar as closed while the terminal is still writing it, and the detection that follows is computed from a candle that later changed. The honest resolution is stated in the remedy: re-run while the symbol is ticking. |
| 125 | CLAUDE.md boundary, `services` (P-2) | `aureon/services` joined `OBSERVER_SIDE` in the architecture guards, so the observer's own services can never import `aureon.execution` or name `order_send`/`BrokerInterface`. It was outside the list only because it did not exist when the list was written. Preflight is why it matters now: it is the first observer-side module to hold a live terminal handle while standing next to a human who is about to enable trading. Verified load-bearing by planting the import and watching both guards fail. The `symbol_specs` check is named `symbol_specs_published` for a related reason — a string constant equal to a bare collection name is what the §83 guard forbids, and a check label is not worth an exemption to a rule that exists because a bare literal fails silently. |
| 126 | §82 session evidence (P-3) | A session is a one-off experiment on a market that will never be in that state again, so what survives it is a document: `docs/evidence/session_{market_date}.md`, generated and committed, filed on the **broker** date (a New York session's last three hours are already tomorrow in Athens, so a UTC name would split one session across two files and neither would be the session anybody ran). It is written in **two halves at two moments**. `scripts/session_run.py` writes the opening half — preflight table, commit, dirty-tree flag, terminal build, broker server, prefix, EMA pair — **before** the observer starts; `scripts/session_verify.py` replaces the closing half after the close. The split is the point: a document written once at the end leaves *nothing* when the observer crashes or the operator gives up, and "we tried on the 16th and something went wrong" is the least useful sentence a research log can hold. So verification preserves every line above the marker byte for byte and only regenerates below it — and re-verifying the next day is normal, because horizons that were PENDING at the close resolve overnight. A session verified without having been bracketed gets a document whose first paragraph says so: it is still worth keeping and it is not the same artefact, since nothing in it attests to a preflight and its commit is the one verification ran at. |
| 127 | §82 session_run refuses, and can be overridden (P-3) | `session_run.py` will not start the observer on a preflight FAIL — a session on a terminal logged into the wrong server costs a market day and produces evidence about nothing. `--force` starts anyway and **records in the document that it did**, next to the failing rows. A gate with no override is a gate people work around by not running it, and an override with a record is strictly better than an unrun check. The stop time is stamped in a `finally`: a crash is exactly the case where nothing else would record when the session ended. |
| 128 | §82 what verification checks, and why six (P-3) | `archive` (the file §82 rests on), `archive_gaps` (an hour missing mid-session still produces a full-looking outcome table, since every horizon spanning the gap is INVALID rather than COMPLETE — the tolerance is the tracker's own, named in one place so the two cannot drift), `live_vs_replay` (the only check that can distinguish an engine difference from a broker one), `detections_stored` (the comparison is also satisfied by both sides producing nothing), `outbox_drained` (pending rows make every stored count a lower bound), and `observer_ran_to_the_close` (the heartbeat against the last archived candle — the failure with no other trace, because a process that died at lunchtime leaves an archive that looks entirely normal and simply stops). Detections with no evaluations are a WARN, not a FAIL: outcomes are written as horizons resolve, so a session verified minutes after the close legitimately has few. Verification reads through `PeriodReader`, which has no write method, so it cannot alter what it is verifying. |
| 129 | evidence marker split (**bug found**) | `replace_after` splits the evidence file on the **marker line**, not on the substring. The first version used `str.partition`, and the document's own header explains what the marker separates and therefore quotes it — so the split matched that sentence, truncating the file to its first three lines on every verification. The test that caught it is the one that asserts the metadata table survives; the obvious assertion, `after.startswith(head)`, passed **vacuously**, because the head had been computed with the same broken split and was cut in the same place. The same shape as decision 116: a test that derives its expectation the same wrong way as the code proves nothing and stays green. |
| 130 | §29-§58 demo drills (P-4) | The demo-account legs of Phases 4, 5 and 6 are nine named drills in `aureon/execution/drills.py`, driven by `scripts/demo_drills.py`: a fill that happens exactly once, the kill switch, two executors racing one request, a stale confirmation, a spread that widened after the confirmation, a lot over the limit, an unknown outcome that reconciles and never retries, a cancel that loses to a fill, and a CLOSED trade refusing to be rewritten. They exist because *"try to make it place two orders"* is not a procedure anybody can repeat identically. Each drill sets up its own preconditions (a drill depending on ambient state would pass or fail for reasons its output does not record), asserts only what an operator could check by hand (a status, a failure code, the number of orders carrying its comment token), and records **every expectation with its observed value** whether it held or not — a table of "ok" with no values is not evidence. They run twice, and the first run is not optional: against FakeBroker and the emulator, which proves the *drills* are right, because a drill that is wrong about what should happen produces a green table beside a broken guard. They live in `aureon/execution` rather than beside the other operator tools because `aureon/services` is observer-side and may not import execution at all (decision 125) — the drills are precisely the code that must hold a broker. |
| 131 | drills that cannot run report SKIP (P-4) | Two refusals, for two different reasons, and both are SKIP rather than a verdict. **Isolation:** the racing drill needs real Firestore transaction semantics, since exactly-once IS those semantics; against a double it skips, so its unit-suite home is the catalogue's shape and its real run is `tests/failure_injection/test_demo_drills.py`. **Scriptability:** drills 5, 7 and 8 need a broker that will widen a spread, drop a connection mid-send or fill a resting order *on request*, and a real terminal does none of those — so against `--broker mt5` they skip, with the manual procedure named in `docs/DEMO_EXECUTION_CHECKLIST.md`. Reporting either as a pass would be the dangerous error: it converts "we did not check" into "we checked". |
| 132 | a drill inherits nothing (**bug found**) | The runner gives every drill a fresh context, and the CLI now builds a **fresh FakeBroker per drill**. The first version shared one broker across the run, and four drills failed as a direct result: drill 5 widens the spread and never puts it back, so drill 6 hit `spread_limit` instead of `max_lot_exceeded`, drill 7's crash-after-send was refused by the spread guard before it could crash, and drill 8's limit order was never placed. The emulator test passed throughout because its fixture builds a broker per drill — so the CLI and the test disagreed about what the drills proved, and only the CLI was right about how an operator would run them. Relatedly, "the broker was never asked to send" is measured from **when the drill's context was built**, not from an empty call log: a real terminal cannot be handed over fresh, so drill 1's fill would otherwise fail drill 2. |
| 133 | §71 emulator without firebase-admin (P-4) | `get_client` falls back to a plain `google.cloud.firestore.Client` when `firebase-admin` is absent **and an emulator host is set**. Without it none of the operator tools — preflight, session verification, the drills — could be rehearsed at all on a machine that has only `google-cloud-firestore`, which is the machine this repository's suite runs on; the emulator test harness had been reaching around `get_client` to build its own client, so the rehearsal story was untestable end to end. Restricted to the emulator on purpose: against a real project the firebase-admin credential path is the one the rest of the system is built on, and quietly resolving a missing dependency through application default credentials is how a tool ends up writing somewhere nobody intended. |
| 134 | §94 phase evidence is derived (P-5) | `docs/PHASES.md` gained an **Evidence** column, and `scripts/update_phases.py` rewrites it from what is on disk: a generated document (optionally required to still contain the section the gate asked for), a session evidence file under `docs/evidence/`, a drill evidence file. `--check` fails when it is stale, and it is in the cross-phase checklist and `make check`. The reason is the direction the old table rotted in: "🟡 demo-account leg outstanding" was a sentence somebody typed, so a phase whose evidence was produced and later invalidated kept its green mark, and nothing in the repository disagreed. A missing artefact renders as `⬜ missing: …` **with the command that would produce it**, which is the more useful half of the cell. Content is checked rather than existence, because the failure mode is a document that is still there and no longer reports the thing: a `PHASE2_BASELINE.md` without its `XAU_OUTCOME_V2` section stops closing Phase 3, and a drill evidence file recording `broker: fake` does not close Phase 4 — a rehearsal proves the drills are right, not that anything ran on a demo account. |
| 135 | the tool does not touch Status (P-5) | `update_phases.py` rewrites exactly one column. Status is a **judgement** — whether a partial pass counts, whether an outstanding leg blocks the next phase (§94) — and a script inferring it from file existence would be making that judgement silently, which is the same class of error as a SKIP rendered like a PASS. It also refuses to run when a catalogued phase has no row in the table, rather than skipping it: an unwritten cell keeps whatever it said before, which is precisely the stale claim the column exists to prevent. |
| 136 | §13-§17 the tuning table is a gate (P-6) | `aureon/config/symbol_tuning.py` now lists **every symbol Aureon will observe**, and the observer refuses to start on one that is absent (`require_tuning`, called for *every* configured symbol at roster build — finding out about the third symbol three hours in is finding out too late). This is a deliberate behaviour change from the silent gold-defaults it had before: an unreviewed instrument on gold's numbers produces detections that *look* like sweeps and are noise, and nothing downstream can tell, because a sweep is a sweep once it is written. An **empty entry is a valid answer** — XAUUSD's says "reviewed, and the shipped defaults are the answer", which is a different statement from "nobody has looked". `tuning_for` stays tolerant on purpose, because the reporting and verification tools are handed whatever symbol they are given and must not explode mid-report; `is_default` is how a reader tells the two cases apart. |
| 137 | XAGUSD is a placeholder of a placeholder (P-6) | Derived by arithmetic from gold's unresearched numbers, so it is one step further from evidence than they are. The derivation is written down so it can be argued with: distance thresholds scaled by the ratio of typical daily ranges (gold ~$30 on ~$2400, silver ~$0.60 on ~$30, so ~50×) and re-expressed in silver's 0.001 tick — 5 points of penetration on gold is $0.05, ~0.17% of its daily range; the same fraction of silver's range is $0.001, which is **one single point**. That result is the finding rather than a comfort: a one-tick penetration is inside the spread on most silver feeds, so expect noise or nothing until somebody researches it. The dimensionless ratios (`min_rejection_fraction`, the wick ratios, `max_close_position`) are deliberately **not** scaled — a wick covering 25% of a candle means the same thing on any instrument, and scaling it would invent a difference rather than correct one. |
| 138 | the broker's tick wins (P-6) | `tuning_for(symbol, point=...)` lets an explicitly passed `point` override the table's, and `default_agents` no longer defaults it to gold's `0.01`. The table's value is what we *expect* the tick to be; `symbol_info.point` is what it *is*, and a caller passing it has read it from the broker. The old default was actively dangerous the moment a second symbol existed: `default_agents(config, point=0.01)` on XAGUSD would have combined gold's tick with silver's thresholds — the worst of both, and nothing in the params snapshot would have looked wrong. |
| 139 | operator documentation (P-7) | `docs/RUNBOOK.md` is the operating half: the five entry points and what each may never do, the daily and weekly commands, how to enable execution, and a section per situation an operator will actually be in — stop trading now, a request stuck in EXECUTING, a position Aureon does not know about, an observer that died mid-session, a growing outbox, a live/replay difference, a symbol the observer refuses. It states **what each tool refuses to do**, because knowing that in advance is cheaper than fighting it at 02:00, and it ends with what none of it proves: the baseline is a synthetic replay, the outcome thresholds are a trader's distance rather than a researched one, and the level parameters are placeholders. |
| 140 | documented commands are compiled (P-7) | `tests/unit/test_docs_commands.py` resolves every `python scripts/…`, every `python main_….py` and every `make <target>` named in the operator documents against the repository, and every documented `--flag` against its script's source. A renamed script turns a runbook from an instruction into a puzzle, and nothing notices, because prose is not compiled — this compiles it. Only **code** is scanned (indented blocks and inline spans), and a make target must *begin* its fragment: a checklist bullet's continuation line is indented exactly like a code block, so "only to make sure you know" was being read as `make sure`. Verified load-bearing by renaming one command and one target in the runbook and watching both halves fail. |
| 141 | §84 one rule per symbol (9A) | `AUREON_EVAL_RULES=XAUUSD:XAU_OUTCOME_V2,XAGUSD:XAG_OUTCOME_V1`, and a multi-symbol config that does not name **every** symbol's rule is refused at construction. Not tidiness: an outcome rule's thresholds are in the instrument's own money, and `XAU_OUTCOME_V2`'s $3–$20 is 10–65% of silver's ~$30 price. Sharing gold's rule would make every XAGUSD horizon read "not reached", and a reached-N table of zeros looks like a finding about silver rather than the unit error it is — the same failure as V1's saturated 100% columns (decision 121), inverted. `AUREON_EVAL_RULE` remains the single-symbol setting and the fallback is available **only** when one symbol is configured, so no existing deployment changes and no multi-symbol one can fall back silently. A malformed `SYMBOL:RULE` pair raises rather than being skipped: a dropped pair leaves its symbol looking unconfigured, and the refusal that follows names the wrong thing. |
| 142 | §21 XAG_OUTCOME_V1 (9A) | Thresholds $0.10 / $0.20 / $0.30 / $0.50 / $1.00 in PRICE, same reference price and same horizons as V2. Gold's distances scaled by the ratio of prices (~80×) and rounded to numbers a silver trader would name, which makes $0.10 on silver roughly the fraction of price that $8 is on gold — so the two rules are **not** comparable threshold-for-threshold, and only their SHAPE is (same reference, same horizons, same COMPLETE-only accounting). Researched on nothing, exactly like V2's own $3–$20, and frozen from the moment it shipped: a better scale is `XAG_OUTCOME_V2`, never an edit. Its `definition_hash` is pinned by a test for the same reason V1's is. |
| 143 | §13–§17 XAGUSD rescaled by fraction of price (9A) | The XAGUSD tuning entry moved from daily-range scaling (~50×, decision 137) to **price-fraction** scaling (~80×): penetration 1.0 (clamped), close-beyond 1.25, min range 2.5, flat band 6.25 points. The clamp is the finding rather than a detail — gold's 5-point penetration is 2.08e-5 of its price, the same fraction of silver's price is **0.625 of one tick**, so gold's placeholder thresholds are *finer than one silver tick* and silver's smallest expressible penetration is already ~1.6× larger in relative terms. The two derivations disagree by that same 1.6× because silver's daily range is a larger share of its price than gold's (~2% vs ~1.25%), and the disagreement is the honest measure of how unresearched both are. The price-fraction numbers are kept because they are the more conservative: a smaller threshold over-selects, and over-selection is visible in a reached-N table where under-selection is invisible. Changing them forks nothing and needs no `agent_version` bump (contrast decision 120): no XAGUSD detection has ever been stored, and XAUUSD's values do not move — which the existing "XAUUSD is unchanged" tests keep true. |
| 144 | §59 system_state per symbol (9A) | `system_state/{symbol}_{timeframe}` rather than one `system_state/current`. The reason only appears with a second symbol: **the write throttle is per document**, so with a shared document gold's candle close resets the timer and silver's state is suppressed for the next few seconds — the symbol a reader is looking at can be stale because of a symbol they are not. Each document carries the global fields (heartbeats, trading_enabled) as well, so reading one symbol is still one read; duplicating them costs nothing and is never the truth anybody depends on, since `heartbeats/{service}` is the source of truth (decision 10) and `settings/execution` is. A state carrying **no** symbols now writes nothing and says so, because its only other fields are derived copies of documents that already exist. `read()` still merges every symbol so the quote lookups on Discord's confirmation screens are untouched by the split, and a merged `updated_at` takes the **newest** — an oldest-wins freshness would make a quiet symbol look like a dead observer. The pre-split `current` document is explicitly skipped on read: merging it would report every symbol twice, once live and once frozen at the split. |
| 145 | test double carries document ids (9A) | `InMemoryFirestore`'s snapshots now expose `.id`, because real ones do and two code paths read it: the detection repository logs it for an unreadable document, and the state repository uses it to skip the pre-9A document. Without it, both paths were being exercised against something the real client does not look like — the same class of gap as decision 116's vacuous assertions. |
| 146 | §75 one engine per symbol (9A) | `SymbolEngines` holds one `AnalysisEngine`, one agent roster and one `LevelTracker` per symbol, and dispatches each candle by the symbol it carries. `AnalysisEngine` already keys its windows and cross counters by `(symbol, timeframe)`, so a single engine would keep the right **history** for two symbols — what it cannot keep is the right **parameters**, because its agents are constructed with one symbol's thresholds and those are not dimensionless (decision 143). `§15/§17`'s shared tracker is preserved *within* a symbol and deliberately not across: "where is the previous day's high" is a different question per instrument. A candle for an unobserved symbol raises rather than being measured with whichever roster was first, and one agent roster handed to a two-symbol observer raises too — passing a list would share the agent instances and their tracker between instruments, which is the exact bug the split exists to prevent. |
| 147 | §22 one outcome tracker per symbol (9A) | Each symbol's tracker holds that symbol's rule **and** that symbol's tick. Both matter: `XAG_OUTCOME_V1`'s $0.10 is 100 points at silver's 0.001 and 10 at gold's 0.01, so a shared point would measure silver's thresholds ten times too small — decision 138's failure again, one layer down. `build_observer` reads the point from the tuning table per symbol rather than relying on `OutcomeTracker`'s 0.01 default, which is gold's tick wearing a default's clothing. |
| 148 | §82 the XAGUSD fixture is not a rescaled copy (9A) | `scripts/gen_fixtures.py` writes both fixtures from one generator, sharing the trading week, the session volatility profile and the trend segmentation — facts about the market rather than about gold — but XAGUSD uses **its own seed**. A rescaled gold series would make every multi-symbol test pass for a reason unrelated to the code: two engines fed the same shape produce the same shape, and a test asserting "the two symbols differ" would be asserting arithmetic. Silver's per-candle volatility is gold's times the ratio of prices times ~1.6 (the same factor decision 143 records, from the other end), and tick **volume** is deliberately not scaled because it is a count rather than a price. XAUUSD's file is byte-identical after the refactor, which its own reproducibility test pins. The replay is a finding in itself: 1063 silver detections against gold's 1172, so the price-fraction scaling makes silver's thresholds about the same fraction of a typical candle as gold's rather than the flood the sub-tick clamp might suggest. |
| 149 | test double served every symbol's candles (**bug found**) | `FakeLiveProvider.get_closed_candles` ignored its `symbol` argument and returned everything it held. With one symbol that is invisible; with two, polling XAGUSD returns gold's candles, the observer routes them by `candle.symbol` straight back to gold's engine, and every multi-symbol test passes while proving nothing about routing. Its `get_quote` and `symbol_info` had the same shape of bug — one symbol's price and one symbol's tick for any symbol asked — which is worse because the result *looks* like a price. All three now answer per symbol, as a real terminal does. |
| 150 | §56 per-symbol execution limits (9A) | `ExecutionSettings.per_symbol` holds a `SymbolLimits` per symbol for `max_lot`, `max_spread_points` and `max_deviation_points`, each optional and falling back to the global value. Two of the three are in **points**, and a point is a different amount of money on every instrument: `max_deviation_points = 20` is $0.20 on gold (~0.008% of price) and $0.02 on silver (~0.07%), so one number tolerates nearly ten times more slippage relative to price on the second symbol. `max_lot` is in lots, and one lot is 100 oz of gold against 5000 oz of silver, so it is a different notional too. The shipped default is **empty** — the global values apply to both symbols, which is what every existing deployment has and is a **known** approximation rather than a researched one. Nothing here invents a silver number; it makes room for one to be set deliberately by a human who can say why. `max_open_positions` and `max_daily_trades` stay global on purpose: they are counts of account-wide exposure, not money measured in points, and a test asserts they have no per-symbol entry to set. |
| 151 | limits resolve as a group (9A) | `settings.limits_for(symbol)` returns a frozen `ResolvedLimits` rather than three separate lookups, and the guard reads `ctx.limits`. A rule comparing a volume against a per-symbol `max_lot` while another compares a spread against the global ceiling would have both believing they read one policy; resolving once makes that impossible. `overridden` names which values came from the per-symbol entry, so a confirmation screen can say a human is not looking at the global policy. Both the refusal path and the deviation **clamp** were verified load-bearing by reverting each to `ctx.settings` separately — they are different code paths, and reverting one left the other's test green, which is exactly why both needed their own. |
| 152 | a guard test that does not pin the clock rots (**bug found**, 9A) | Three of 9A-4's per-symbol tests called `execution_guard.check(...)` directly instead of the file's `verdict(...)` helper, which passes `now=NOW` — the timestamp captured when the module is imported. `rule_quote_fresh` measures the broker quote against `ctx.now`, and the quote helpers pin `captured_at=NOW` too, so a direct call let the clock run: fifteen seconds of wall time after import (`quote_ttl_seconds`) and every one of them refuses with STALE_QUOTE. They passed alone and failed in the full suite, where they run twelve minutes in — the shape of failure that gets blamed on flakiness and re-run until green. They now go through `verdict(...)`, and an AST test asserts `check` is called exactly once in the file, by that helper, with `now=` given: the mistake is cheap to repeat and invisible until the suite is long.
| 153 | §37–§42 `/execute` shortens the typing, never the authorisation (9A) | `/execute symbol side lot [detection]` sits beside `/execute-trade` rather than replacing it: the wizard is the right shape for a pending order placed deliberately, and the wrong shape for the thing done most often — a market order on a symbol already being watched, five options deep for one decision. What is **not** shortened: the lot is typed (no default and no "same as last time" — it is the one field where a wrong guess costs money), CONFIRM still goes through the same transactional REQUESTED → CONFIRMED path with the same owner check, staleness re-prompt and audit, and the filling mode is **named** (FOK preferred; IOC accepted with an explicit note that it may fill part of the volume; neither available → refused, because §39's point is that the mode is shown and an unnamed broker default is not shown). Every rule lives in `service.plan_market_order`, which returns a plan rather than early-returning inside the command, so each refusal is tested without a Discord interaction; the command is transport. A refusal produces **no** `trade_requests` document, which is the property an emulator test asserts: a rejected shortcut that still wrote a REQUESTED document would leave something a human could confirm later, which is the opposite of a rejection. |
| 154 | §50 the shortcut's detection link is by id, never by proximity (9A) | `/execute-trade` offers the most recent linkable detection; `/execute` takes a `detection_id` typed or carried by a notification button (9C), which makes the link an explicit statement by a human rather than a guess from timing — and §50 keeps inferred links out of trade requests entirely. A detection whose symbol differs from the order's is **refused**, not attached and not silently dropped: a link that misdescribes what was acted on poisons every review built on it, and with two symbols observed in one process the mistake is now one autocomplete away. |
| 155 | a wrongly-priced test broker makes a safety test vacuous (near-miss, 9A) | The first run of the `/execute` flow tests confirmed a silver order and then executed **nothing** — not because Discord had been prevented from trading, but because the suite's shared `FakeBroker` quotes gold: the guard compared a quote confirmed at $30 against a live price of $2400 and refused it at `max_deviation_points`, *after* CONFIRM. A test whose assertion had been "nothing reached the broker" would have passed for that reason and proved nothing about the shortcut at all — decision 149's shape again, in the broker rather than the provider. These tests use a broker priced in the symbol under test, and assert the execution **count** after CONFIRM as well as before, so the difference between "refused" and "never sent" cannot hide. |
| 156 | §46–§47 `symbol:` on a cancel or close is a cross-check (9A) | A broker ticket is eight digits nobody can read, and on a two-symbol account one wrong digit names a real order belonging to the other instrument — the request then succeeds, against the wrong trade. Naming the symbol turns that into a refusal: Discord looks the target up in Firestore (a close by `mt5_position_id` among imported trades, a cancel by `order_ticket` among the requests that placed one) and refuses a mismatch. It refuses an **unverifiable** claim too, because a guarantee that lapses exactly when the record is missing is not one; the unchecked action stays available by omitting `symbol:`, which is then plainly a deliberate choice. The asserted symbol rides on the `control_requests` document and the executor re-checks it against the live position or order before touching the broker, because Firestore and the broker can disagree and the broker is the account that matters. Only a symbol a **human** asserted is sent: attaching one Aureon merely inferred would let a stale record block a close, and a close is the risk-reducing direction. A target the broker does not report at all is not refused by the worker — that is the market having won the race, and the broker must say so in its own words (§46). |
| 157 | `/close symbol:` will not choose between two positions (9A) | The shortcut exists for the common case: one position on that symbol, close it, no ids to copy. With two, picking the newest, the largest or the first would be Aureon deciding which of a human's trades to end, so it lists the position ids and asks for `/close-trade`. With none it says so rather than reporting a no-op as a close. |
| 158 | §59 `/status symbol:` reads that symbol's own document (9A) | Not a filter over a merged picture: the scoped screen reads `system_state/{symbol}_{timeframe}`, so the freshness shown is that symbol's, and the open-trade and pending-request counts are its own. A screen that narrowed the panels but kept both symbols' counts would be the worst of the two, because the count is the number a trader reads to decide whether they are exposed. The scope is in the **title** rather than a footnote, and which of the two modes renders (§59 live panels vs §61–§63 review) still follows the market state of the symbol asked about. |
| 159 | a test double with the wrong field name (near-miss, 9A) | `resolve_close_target` was first written against a stand-in trade carrying `position_id`, and passed. No `Trade` has that field — the broker's id is `mt5_position_id` (§49) — so against real documents the rule would have found nothing and reported "no open position" for every symbol: the most reassuring possible way to be wrong. The helper now builds a real `Trade`, and a test asserts the attribute the double invented does not exist. Third instance of this shape (decisions 145, 149), and the first where the double was written in the same commit as the code it validated.|
| 160 | §45 excursions use the position's own tick (9A) | `PositionMonitor` had one `point` for the process, passed to `ExcursionTracker` and to the candle reconstruction. Excursions are in POINTS, so the divisor decides what the number means: gold's 0.01 against silver's 0.001 makes one shared tick a **tenfold** error, and a silent one — 30 and 300 are both plausible MFEs, and nobody can sanity-check either by eye. The tick now comes from the **broker's** spec per symbol, cached per session. The broker rather than `symbol_tuning.py`, because the account can hold a hand-opened position on an instrument Aureon does not observe (§52) and the terminal knows every symbol's tick while a tuning table only knows the reviewed ones. A symbol whose spec cannot be read is left **unmeasured** rather than measured with a fallback: an absent excursion is a gap in a diagnostic, a wrong one is a figure a review reports as fact. `track()` runs on every poll, so a briefly unreadable spec resolves itself. External positions are still imported on **any** symbol, observed or not — a trade the human opened is part of the account's exposure, and a monitor that hid it would make `/status` read as flat while money was at risk. |
| 161 | the broker double served one instrument to every symbol (**bug found**, 9A) | `FakeBroker.symbol_info` returned its single spec with the symbol relabelled, `quote` returned one bid/ask for any symbol, and — worst — `_send` priced every market fill from that global book. So a silver order **filled at gold's price**: the first two-symbol monitor test computed an excursion of -2,370,000 points, which is the only reason the bug was visible at all. Had the numbers been closer, every multi-symbol execution and monitor test would have been measuring gold while asserting about silver. It now takes `symbols=` and `prices=` maps, prices fills and closes from the symbol's own book, and once a symbols map is given an unknown symbol **raises** as a terminal does for a symbol not in Market Watch — so a test cannot be served gold by accident. Third instance of the doubles-don't-look-like-reality failure (145, 149, 159), and the most consequential: this one is money-shaped. |
| 162 | §61–§63 one review per symbol (9A) | `daily_reviews/{date}_{symbol}` and `weekly_reviews/{year}-W{week}_{symbol}`, each generated with **that symbol's** frozen rule. Aggregating both into one document would put a single `evaluation_rule_id` on numbers produced by two rules (decision 141) and add horizon reached-counts measured against thresholds denominated in different instruments' money — a table that looks comparable and is not. The shape is identical, so a reader compares two documents instead of reading one that quietly mixes two things. The symbol is a **suffix** on the id, keeping the chronological string sort that makes "the latest review" an index-free maximum (decision 26), and `latest_weekly(symbol)` narrows by the stored `symbol` **field** rather than by parsing ids: an id is a naming convention, the field is the document's own statement about what it measured. Filtering the period happens in Python, for the same reason the rest of the review loading does — a second query field would need a composite index declared and deployed for a batch job that runs a few times a day. `symbol=None` still means "everything in the period", which is what every pre-9A review says and must keep parsing. `main_review.py` generates every configured symbol by default and continues past a symbol that fails: a missing silver review is not a reason to have no gold review. |
| 163 | a pre-9A review will not appear under a symbol (9A) | Reviews generated before the split carry `symbol=None`, so `/status symbol:XAUUSD` does not show them and reports "no review yet" instead. Deliberate: the alternative is to treat an unlabelled aggregate as though it were one symbol's, which on a two-symbol deployment would be wrong, and on a one-symbol deployment would hide the upgrade rather than surface it. Regenerating is the remedy and is safe by construction — a review is a pure function of the data it aggregates and re-running overwrites the same document — so the runbook says to regenerate the periods that matter. |
| 164 | §82 one evidence file per symbol per session (9A) | `docs/evidence/session_{market_date}_{symbol}.md`. Every check inside is about one instrument — its detections, its archive, its live-vs-replay comparison, its outcome rule — so two symbols sharing a file would mean the second verification silently replacing the first's verdict, under a header naming one of them. No evidence file existed yet when this landed, so the convention changed cleanly; the helper still addresses an unsuffixed name so a pre-9A file could be read if one appeared. |
| 165 | the verifier and preflight ask about the symbol they were given (9A) | `SessionVerifier` looked up `config.evaluation_rule_id` for its evaluations, so verifying a silver session would have searched for `XAU_OUTCOME_V2` evaluations, found none, and reported **"0 detections evaluated under XAU_OUTCOME_V2"** — which reads as a broken evaluator rather than as a verifier asking the wrong question, and would have been blamed on the observer. Both it and `preflight`'s config line now use `config.rule_id_for(symbol)`. The verifier's tick was already per symbol (P-6); this closes the other half of the same dimensional gap. |
| 166 | the XAGUSD baseline section measures its own rule (9A) | `docs/PHASE2_BASELINE.md` gained an XAGUSD section of the same shape as gold's, replayed from `XAGUSD_M5.csv` through the observer's own `default_agents(symbol="XAGUSD")` and evaluated under `XAG_OUTCOME_V1`. Its diagnostics block is **generated**, comparing each rung as a fraction of price beside gold's and printing what each one actually caught, and it reports a finding the arithmetic alone did not predict: silver's fourth and fifth rungs ($0.50, $1.00) are reached in 0.1% and 0.0% of COMPLETE horizons where gold's are reached in 2.5% and 1.5%. The cause is decision 142's rounding — every silver rung sits at 2.5–4× gold's fraction of price, and the top of the ladder is past what the fixture's volatility can reach. The rule is frozen (§21), so this is recorded rather than fixed: a better ladder is `XAG_OUTCOME_V2` evaluated alongside, the same discipline that kept `EMA_OUTCOME_V1`'s saturated columns in the document (decision 121). |
| 167 | parity, outbox, recovery and evaluation are parametrized over both symbols (9A) | The four properties Phase 2–3 proved for gold now run for silver too: archive→replay parity (§82), fifty enqueues through an outage delivering exactly once (§83), a restart producing neither a duplicate nor a gap (§75), and every directional detection evaluated under that symbol's own rule. Each parametrized case asserts the symbol actually produced detections first, so a fixture that stopped selecting anything would fail rather than pass vacuously. Two of them also assert the shared paths keep the symbols apart: one outbox file carrying both symbols' rows without collision, and one archive directory reading back each symbol's own candles. |
| 168 | §19 a volume profile stores its inputs beside its outputs (9B) | A POC on its own is unfalsifiable: two implementations produce two different ones from the same candles and both look plausible, because the answer depends entirely on the bin width, the scope and how a candle's volume is spread across its range. So `VolumeProfile` carries `scope`, `start_utc`, `end_utc` and `bin_points` next to `poc_price` and the value area — a POC over the Asia session at 10-point bins and one over the day at 2-point bins are different facts wearing the same field name. The volume itself is **estimated**: an M5 candle reports one `tick_volume` and says nothing about where inside its range it happened, so it is spread across the bar's high–low in proportion to each bin's **overlap** (not one equal share per bin, which would move the POC whenever a wide bar straddled a boundary). The assumption is wrong in a knowable direction — a bar that sat at one extreme is reported as though it spread evenly — and real ticks would answer properly, which CLAUDE.md forbids storing. A doji is the one case where it is exactly right. Bins are capped at 200, keeping the busiest, while `total_volume` still describes the whole scope. |
| 169 | a bin index computed as `price / width` lands on the wrong bin (**bug found**, 9B) | `math.floor(2400.10 / 0.10)` is 24000, not 24001: the quotient is 24000.999999999996 in binary, so **every exact bin edge fell one bin low** and the profile's whole shape drifted by a bin. Nothing about a POC assertion would have revealed it — the first version's hand-built tests passed, because `_spread_candle` skips zero-overlap bins and quietly put the volume in the right place anyway. The index is computed in **ticks** now (`round(price / point)` then integer division by `bin_points`), which is exact for every price a broker can quote, and the edge price is rounded to the instrument's precision because `24001 * 0.1` is 2400.1000000000004. The test that catches it asserts the property rather than a value: two adjacent edges must be two adjacent bins. |
| 170 | 9B's thresholds are placeholders too, and the bin width is per symbol | `volume_bin_points` (10 gold, 2 silver) and the volatility regime bands (0.70 / 1.40 of the 20-day median) live in `symbol_tuning.py` beside the level and wick placeholders, because a bin width decides what a *node* is and a regime band decides what *high volatility* means — choices about resolution, not measurements. The width is in points and therefore instrument-specific: 10 points is $0.10 of gold and $0.01 of silver. Silver's 2 comes from the same price-fraction arithmetic as decision 143 (1.25 ticks) rounded **up** rather than clamped down: a one-tick bin would make every candle its own node on an instrument whose daily range is ~600 ticks, and a 600-bin profile has no peaks to read. The value-area fraction (70%) is NOT per symbol — it is the definition of a value area, not a parameter of an instrument — and the regime bands are dimensionless ratios, so they are not scaled either. `VOLATILITY_BANDS_VERSION` is stamped on every stored context: a label whose definition moved silently is worse than no label, because every comparison across the change is then wrong without appearing to be. |
| 171 | §19 volatility is a measured ratio with a versioned label (9B) | `atr(candles, 14)` reuses `indicators._wilder`, so ATR and RSI share one recursion and one warm-up convention — a second implementation of the same smoother is how two numbers that should agree stop agreeing. Below warm-up the answer is **None**, not a partial average: with thirteen candles there is no 14-period ATR, and a number computed from what is available would be read as one. True range includes the previous close, because a Monday gap is real movement a bar's own high–low cannot see. `session_range_vs_median` is this session's range over the **median** of the same session across the previous 20 broker days — the median rather than the mean, because one news day would drag a mean and make every session after it look small. `regime` is then a label over that ratio from bands in `symbol_tuning`, and `bands_version` is stored beside it; the version is stamped **only** when there is a regime, since a version on an absent label would suggest a judgement was made. Both the fraction-of-ATR figure and the points figure are reported: the fraction is comparable across instruments, and the operator reading `/status` thinks in points. |
| 172 | §19 a detection's context is ASIA's profile, and it is attached in the engine (9B) | `Detection.volume_profile_ref` and `Detection.volatility` are computed at the candle's close from candles that had **already closed**, and never recomputed. The reference's scope is **Asia** for three reasons that point the same way: the overnight range is what the rest of the day trades against (which is why §19's tags are written in terms of it), Asia's profile is retained until the broker day ends so the field means the same thing at 09:00 and at 22:00, and a *developing day* profile would not — at 09:00 "today's value area" IS Asia's, by 17:00 it is everything, and one field name meaning two things at two hours is the worst property a research field can have. Before Asia has traded the reference is **None**, not an empty profile: absent context is a gap a reader can see, where an empty one looks like a measurement that found nothing. The attachment happens in `AnalysisEngine`, not in the agents: an agent is a pure function of `(window, ctx)`, and reaching for a day of candles inside one would break the replay/live parity the engine rests on — and attaching centrally guarantees every agent's detections from one candle carry the same figures, computed once. |
| 173 | a detection's context is a function of the BROKER DAY, not a rolling window (9B) | ATR and every profile a detection records come from the current broker day's candles. This is a **parity** property, not a preference: `session_verify` compares a live session's stored detections against a replay of that day's archive (§82), and a rolling 24h buffer would give the live run — whose reach-back may load candles from before the archive begins — more history than the replay, so every context field in the first candles would differ and the comparison would report a difference that is an artefact of where the archive starts. The cost is stated plainly: ATR is absent for the first fourteen candles of a broker day rather than borrowing yesterday's. An absent number for seventy minutes is better than one nobody can reproduce. `rolling_24h` survives as a profile scope for the live panel, where it is labelled and never compared. |
| 174 | every agent's version moved, not only the four the phase named (9B) | 9B listed `ema_cross` 2.1.0, `liquidity` 1.1.0, `breakout` 1.1.0 and `wick` 1.1.0. `rsi` and `session_trend` were bumped to 1.1.0 as well, because the reason to bump is that the two populations must be **separable** (§12) and their documents changed shape identically — the engine attaches context to every detection, not to a chosen four. An agent left at 1.0.0 would have pre-9B and post-9B detections sharing ids with nothing to tell them apart, which is exactly the failure decision 16's reversal exists to prevent. |
| 175 | the context costs 12% of a replay, measured rather than assumed (9B) | A first reading of the agent suite's runtime (163s) looked like the new per-candle work; measuring it showed the suite was already that slow and the context adds **1.6s to a 13.5s week** (1172 detections, whole roster). The profile and the volatility are cached per candle so N detections from one candle cost one computation, which is the change worth having; a streaming accumulator would be a larger refactor with two implementations of the same arithmetic to keep in step, and 12% does not buy that risk. Recorded because the first instinct was to optimise the wrong thing. |
| 176 | §59 the state document carries profile SUMMARIES, on the symbol (9B) | `SymbolState.volume_profile` holds `current_session | asia | day -> ProfileSummary` and `SymbolState.volatility` holds the context. On `SymbolState` rather than `SystemState` because after decision 144 the state document **is** per symbol and a profile is a fact about one instrument. `ProfileSummary` is its own model rather than a `VolumeProfile` with `bins=()`, because the two say different things: an empty `bins` tuple on a profile means *nothing traded*, and the summary means *the shape is not carried here* — confusing them is how a panel comes to report a quiet session for a busy one. The observer reads both from the engine's tracker rather than recomputing them, for the same reason it reads the cross counts from the engine (decision 65): a second computation would eventually disagree with the one the detections were stamped with, and the panel would then describe a market the stored detections never saw. |
| 177 | the §19 panel renders its rows empty rather than hiding them (9B) | The value areas, the nearest nodes and the ATR line appear whether or not they have values, like every other row on the live panel: a block that appears only when populated changes the panel's shape as data arrives, and a reader cannot tell *no profile yet* from *this panel never had that row*. "Nearest" node is computed against the **current bid** rather than stored, because where price is now is the whole question and a stored answer would be as old as the last candle. |
| 178 | §19/§23 the volume and volatility tags read the DETECTION, not the world (9B) | `price_above_asia_va`, `price_below_asia_va`, `cross_at_lvn` and `cross_at_poc` are derived from the detection's own stored `volume_profile_ref`, which was stamped at its candle close. That makes the no-hindsight rule **structural** rather than enforced: there is no later data to reach for, because the only data is what the immutable document holds. A detection with no reference — the first candles of a broker day, before Asia has traded — is **false everywhere** rather than "inside the value area": reporting the unmeasured as measured is how a group of unknowns ends up counted as a finding. "At" a node means within half a bin of it, so the tolerance is the instrument's own resolution: a fixed $0.05 would be half a gold bin and fifty silver ones. |
| 179 | `volatility_regime` is three tags, not one (9B) | The phase names one tag; the tag map is `dict[str, bool]` by contract, and a string-valued entry would change the type for every consumer of every stored evaluation. `volatility_regime_low|normal|high` groups identically and keeps the contract: exactly one is true when the regime is known, and **all three are false when it is not** — which is its own group, since an unwarmed context is not a normal session and must not be counted as one. The weekly review picks them up without further change, because `compare_by_tag` defaults to `CONTEXT_TAGS`. |
| 180 | §19 the baseline reports outcomes split by context, both sides (9B) | `docs/PHASE2_BASELINE.md` gains an "outcomes by volume and volatility context" table per symbol: for each 9B tag, the reached rate among detections carrying it **and** among those not, over COMPLETE horizons only. Both sides always, because a `with` figure alone is unreadable — 60% reached means nothing until the `without` number is 30% or 65%. The horizon and threshold are named in the heading rather than left implicit, since a reached-N split is about one threshold and a table that hides which one invites reading it as all of them, and a split with too few COMPLETE horizons on one side is marked rather than printed as a percentage of nothing. The numbers are anecdote and the prose says so: one synthetic week, and the largest visible gap (gold's `volatility_regime_low`, 7% with against 14% without) rests on a few hundred horizons of a random walk. |
| 181 | 9C a notification's ID is its dedup, and the claim comes BEFORE the post | `{prefix}_notifications/{kind}__{ref_id}`, written with `create` (which Firestore refuses over an existing document). Keeping it as a document rather than an in-process set is the whole point: a set is empty after a restart, which is exactly when a duplicate would be sent. The order is claim-then-post, and the safe direction is not obvious — claiming first can **lose** a message if the process dies in between, posting first can **duplicate** one. A lost detection embed costs a human a glance at `/status`; a duplicated price alert tells them twice that a level they are watching has been crossed, which is a reason to act, twice. A failed post is recorded as FAILED for the operator and **not retried**, because a retry over a channel that is rejecting messages either double-posts or hides the outage. |
| 182 | 9C an alert is answered once, transactionally, and by the observer | `PriceAlert` ARMED → FIRED happens inside a Firestore transaction that re-reads the document and refuses unless it is still ARMED, so two observer instances seeing the same crossing produce **one** message — the executor's claim discipline applied to a message. FIRED, CANCELLED and EXPIRED are terminal (`PRICE_ALERT_TRANSITIONS`, gated by `assert_price_alert_transition` per CLAUDE.md): an alert that has fired is a fact about a moment, not a switch, and re-arming is a new alert so the record of what was asked and when it was answered stays one fact. **The observer fires it**, not Discord, because firing needs the quotes the observer already reads and Discord may not call the broker. Crossing is tested against the side of the book a trader would transact at — an alert for 3700 from below is answered when they could have *bought* at 3700 — because the mid would fire half a spread early, every time, in the direction that flatters the alert. |
| 183 | 9C the fired snapshot is frozen by the writer, rendered by Discord | `fired_snapshot` is filled in the same transaction that sets FIRED, by the process that has the indicators. Discord renders that copy and never recomputes it: rendering from live state would describe a market seconds after the one that crossed the level — a small lie that is impossible to notice and impossible to reconstruct. It is a plain dict for the reason `SymbolState.last_*` is: the fields are a human's line of text rather than an input to a calculation, and a typed sub-model would have to be extended in lockstep with every agent. |
| 184 | 9C the notified agents are `liquidity`, not `liquidity_sweep` | The phase's default list names `liquidity_sweep`; no such agent exists — the agent is `liquidity` and its event keys carry the sweep's direction. Used the real name, as with the same slip in the drills earlier. The defaults are the four an eye watching a chart would notice (cross, sweep, break, rejection wick); `rsi` and `session_trend` are deliberately absent, because an alert for every RSI reading is an alert for nothing. They live in `settings/notifications` rather than the environment for decision 11's reason: silencing a noisy agent at 02:00 must take effect on the next detection, without a redeploy. |
| 185 | 9C `/remind` refuses a level that is already behind the market | An alert to be told when price goes *above* a level that is already below the market has happened: it would fire on the next quote, which is not a reminder, it is an echo. Silently arming it would deliver a useless notification seconds later and teach the trader to distrust the channel, so the refusal carries the **current price** — which is how somebody notices they typed `above` for `below`. The check uses the same side of the book the firing will (`ask` for `above`, `bid` for `below`), so the two cannot disagree about a level inside the spread; such a level is refused from **either** side, because it genuinely has been reached whichever way it is named. The per-user cap is checked before the quote is even read: it is about the channel being usable, not about the market. |
| 186 | 9C the observer answers alerts, on the poll clock | Firing lives in the observer because it is the process with quotes: Discord may not call the broker (CLAUDE.md), and a second MT5 connection polling levels would double the terminal's load for no new information. It runs on the **poll** clock (a new `MarketEngine.on_poll` hook, ~1s) rather than on candle close, because "the first quote across the level" is what a human asked about and a five-minute granularity answers a different question — one that may already have reversed. The check is cheap in the common case: the armed-alert query comes first and **no quote is read at all** for a symbol nobody is watching a level on, which a test asserts through the provider's own read counter. Expiry runs on candle close, so it shares the clock everything else the observer records uses and needs no second scheduler. A Firestore outage in either path is logged and swallowed: alerting is a side errand and must never stop observation. |
| 187 | 9C the frozen snapshot is the `/status` panel's field set | `build_snapshot` takes `MarketSnapshot.as_state()` plus 9B's profile summaries and volatility, so a human reading a fired reminder and a human reading `/status` are comparing the same fields rather than two definitions that drift. Two fields are computed at firing rather than copied: `ema_relation` (the sign of a distance is the bias, and a reminder is read in a hurry) and `minutes_since_cross` — the one value that depends on *when the alert fired* rather than on what the market did, so a stored "4 minutes ago" would be worse than storing nothing. The whole snapshot is JSON-serialisable by test, because Firestore stores it and Discord reads it back. |
| 188 | a provider double that cannot be observed makes a test vacuous (near-miss, 9C) | The first version of "no armed alert costs no quote read" asked `getattr(provider, "quote_calls", None)` and skipped its assertion when the attribute was absent — which it was, so the test passed while checking nothing (decision 116's shape again). `FakeLiveProvider` now counts quote reads, and the test asserts the real numbers: **zero** reads with no armed alert, one with an alert armed. "We did not do the expensive thing" is invisible unless the double can be asked. |
| 189 | 9C an announcement polls Firestore rather than listening to it | The Python client offers snapshot listeners; the notifier sweeps every few seconds instead. A listener's callback runs on the client's own thread and posting from it means hopping back to the event loop anyway; more importantly a dropped listener **reconnects silently** and the window it missed is gone, where a poll that fails simply runs again over the same window. The window is two minutes: long enough to survive a restart or a slow write, short enough that a bot down for an hour wakes and posts nothing rather than an hour of history — a channel full of stale detections is worse than a gap, because the gap is visible. `AUREON_NOTIFY_WINDOW_SECONDS` names it, `AUREON_ALERT_CHANNEL_ID` the channel; without a channel the bot **announces nothing** and says so in the log, rather than picking one it can see. |
| 190 | 9C [Execute] asks for the lot; nothing else about the confirmation is shortened | The button opens a modal with one required field and no default, and the typed lot then goes through the same `plan_market_order` → `ConfirmTradeView` path as `/execute`. A button carrying a size would make the most consequential number in the system a thing somebody tapped rather than typed, and "same as last time" is that with a memory. Symbol and side **are** prefilled: they are what the embed is about, so taking them from the detection removes a retyping error rather than a decision — and a context-only detection (no direction) has the button removed entirely rather than defaulting to buy. The allowlist is checked on the press, because the channel is readable by more people than may trade (§71). An emulator suite drives the real button, the real modal and the real CONFIRM with a `FakeBroker` watching, and four plants (a modal that ignores what was typed, a button that skips the modal, a missing allowlist check, a context-only view that keeps the button) each turn it red. |
| 191 | 9C the embeds are neutral in colour, deliberately | `notification_embed` and `reminder_embed` both use the info colour. A green embed for a buy detection and a red one for a sell would read as approval — the interface would be making the call the whole design refuses to make — and the eye reaches the colour before the words. Every announcement carries the same "research only · not a recommendation" footer for the same reason, and `[Monitor]` points at `/monitor` rather than rendering a second assessment, because two renderings of "how is this doing" drift and the one inside a notification would be the one nobody updated. |
| 192 | a test double that ignores a query operator makes every windowed read vacuous (near-miss, 9C) | `InMemoryFirestore` applied only `==` filters and silently dropped everything else, so "a detection older than the window is not announced" passed while the window did nothing — and `order_by` was ignored while `limit` was applied, which in a "newest first, limit N" query returns the **oldest** N and looks right in a fixture written in order. The double now implements the range operators, resolves dotted field paths (`detected_at.utc` addresses a value inside a map, so `doc.get()` alone found nothing), sorts before limiting, and **raises** on an operator it does not implement rather than matching everything. Same failure mode as decisions 116 and 188: a double that cannot be asked about a property will report that the property holds. |
| 193 | "every frozen field reaches the embed" is only a test if the match cannot be satisfied by a neighbour (near-miss, 9C) | The coverage test builds a real snapshot with `build_snapshot` and asserts each key is visible in the rendered reminder — the right shape, and it passed three deletions in a row while proving nothing. `detections_today = 3` was found inside the `2438.00` of the session range (a bare substring match), and deleting the whole "Last sweep" row was covered by a `"buy"` the neighbouring cross row happened to render (a dict satisfied by **any** inner value). Matching is now bounded by digits and dots — "42m ago" still counts as a rendering of 42 — a dict must have **all** its values shown, and the fixture gives each event distinct ones. Found by planting the four deletions, not by reading it. |
| 194 | 9D the trend read is computed by the OBSERVER and published; Discord only reads it | `/monitor`'s trend read is over the last 60 closed candles, and `BotContext` may hold no data provider — a boundary test asserts it by inspecting the dataclass's own annotations, precisely so the object cannot arrive at runtime. So `assessment_service.read_trend(candles, …)` is called by the observer, which has the candles, and its result is published into that symbol's `system_state` document; `/monitor` reads it from Firestore like every other number it shows, and `assess()` takes the `TrendRead` as an argument rather than deriving it. Same module the phase names, and "Discord never computes indicators" stays literally true rather than true-by-convention. The cohort half is a Firestore read, which Discord may do. |
| 195 | 9D an assessment is never scored in place | The weekly review asks whether price reached the TP quantile before the SL quantile, and records `assessment_hit_rate` on the **review**. Nothing writes an outcome back onto the stored `Assessment`. Writing it back would make a document say something different from what it said when it was written, which is the failure §21 freezes evaluation rules to avoid — and it would destroy the only comparison worth having, between what was believed at the time and what followed. For the same reason `new_assessment_id()` is random rather than derived from the detection: the same detection assessed an hour later has an hour more history behind it, and those are two statements about two evidence bases, not a correction of one by the other. |
| 196 | 9D Wilson, and one named quantile definition | The textbook interval `p ± z·sqrt(p(1-p)/n)` runs outside [0, 1] and is exactly zero wide at p=0, so "0 of 30 reached it" would publish a confident 0% with no uncertainty at all — the opposite of what printing an interval is for. Wilson stays inside the unit interval and keeps a width at the extremes. Quantiles use linear interpolation between closest ranks (R type 7, numpy's default, `statistics.quantiles(method="inclusive")`), pinned against the stdlib at n = 2, 5, 40, 41: a p75 of forty numbers has at least nine standard definitions that disagree by a rank or two, which at these thresholds is a dollar or two of gold. Both live in `aureon/evaluation/stats.py` so the number in a Discord embed and the number in a weekly review cannot drift. The bounds are rounded to six places before clamping, because at p=1 the algebra gives exactly 1 and the floating point gives 0.9999999999999999 — which clamping alone does not catch, since the value really is below 1. |
| 197 | 9D the cohort gives up its narrowest dimension first, and says so | `WIDENING_ORDER = (wick_tag, price_vs_va, volatility_regime)`: the wick tag says something about one candle, the value-area position about one session's shape, and the volatility regime about what a distance even means — $5 of gold is a common excursion in a high-volatility session and a rare one in a quiet one, so it is surrendered last. Symbol, agent, direction and session are never dropped at all: a cohort mixing them would be answering about a different instrument, a different signal or a different hour while wearing the same words. What was dropped is recorded on the `CohortFilter` and rendered, never silent. Below `MIN_COHORT = 30` COMPLETE evaluations even fully widened, no percentage is published — the assessment is still stored, because "we looked and there was not enough history" is a finding worth being able to count later. |
| 198 | 9D notes live beside the trade, and nothing automated reads them | A CLOSED trade refuses every field write but the reconciliation stamps (§45), and a note is not a reconciliation — so `/note` writes `{prefix}_trade_notes/{note_id}`, its own collection rather than a subcollection, so no code path that walks a document's children can mistake it for part of the trade. A test plants a write to the trade document and turns red. `#tag`s are parsed from the text at read time rather than stored, because a tag is part of what the human wrote. The cohort, the assessment and every gate ignore notes entirely: the moment a note moved a number, the number would stop measuring the market and start measuring the trader's mood when they typed it, and nothing downstream could tell those apart. |
| 199 | 9D a strictly monotonic series has NO swing pivots, which a staircase fixture hides (near-miss) | The structure count uses the fractal definition: a pivot high clears two candles on each side. Written that way, a clean rising staircase has **zero** interior pivots -- every candle is exceeded by the next -- so the first fixture, a monotonic ramp, reported no higher highs at all for the most obviously trending data imaginable, and the test asserting "a rising window reads bullish" would have passed on the other four votes while proving nothing about structure. The fixtures are now zigzags with a drift (`zigzag(drift=±0.4)` to trend, `drift=0` to chop), which is also what a market looks like. Found by printing the counts rather than by reading the code. |
| 200 | 9D an EQUAL swing pivot counts as neither higher nor lower | The first version wrote `counts["hh" if later > earlier else "lh"]`, which puts every equal pivot in the bearish bucket -- so a flat range, the exact case the SIDEWAYS verdict exists for, reads as a downtrend. Two swings at the same price is a level being retested. Equality now counts as neither, and the neutral fixture is a zigzag with real pivots that happen to be equal rather than one with no pivots at all: a fixture that scored zero for want of any pivot would have been neutral for a reason that proves nothing. |
| 201 | 9D the cohort may not contain detections made after the one being assessed | `population_from` takes the subject's own `detected_at` and drops everything at or after it. This look-ahead is far easier to introduce here than in the evaluations themselves: the rows are simply sitting in a collection, and nothing about reading them says when they happened -- so a cohort assembled by a query alone would quietly answer "what followed detections like this?" using detections that had not occurred when the question was asked. A detection whose horizons are all PENDING contributes nothing either, per §22: treating unknown as failure is the easiest way to make a strategy look worse than it is. A dimension the *member* cannot answer fails the match rather than passing it, for the same reason -- counting an unknown as agreement is how a cohort fills up with detections never shown to have the property being asked about. |
| 202 | 9D Discord gets an `EvaluationReader`, not the repository | `/monitor` has to read `detection_evaluations` to count how often a cohort reached a threshold. Handing `BotContext` the repository turned the existing boundary test red on its first run — the repository can `upsert`, and an evaluation is the measured record of what followed a detection, so a human interface able to rewrite one would make every number built on those rows unfalsifiable. `EvaluationReader` has `get` and `get_many` and no write method, exactly as `ReviewReader` does for §61, and the capability split is visible in the import graph rather than resting on nobody calling the wrong method. Assessments and notes are full repositories by contrast: both ARE Discord artefacts, created by `/monitor` and `/note` and by nothing else. |
| 203 | 9D the estimate horizon defaults to the last COUNTED horizon, not the last one | The rule's horizon tuple ends with `opposite_cross`, which is event-driven: it stays PENDING until the EMAs cross back, which on a trending day is hours away and on a quiet one may never happen. Defaulting the TP/SL quantiles to `rule.horizons[-1]` produced a readout with a cohort of **thirty-five** and no estimates at all, every row reading `0/0` — a screen that looks like a measurement of a quiet market rather than a measurement of the wrong column. `default_estimate_horizon` now takes the last horizon of kind CANDLES or MINUTES (`m60`). Found by running the end-to-end test, not by reading the code; the fixture that populated a single horizon was the other half of the same blind spot, and now populates every counted one as a real evaluation does. |
| 204 | the shared `embed_text` helper could not see a footer (near-miss, 9C+9D) | It joined the title, description and fields and stopped there. "research only · not a recommendation" (9C) and "measured from n=… · not advice" (9D) live in the **footer** and nowhere else, so nothing driven through that helper could assert the one line separating a readout from a recommendation. It surfaced as a failure rather than a false pass — the first `/monitor` test to check for "not advice" failed against a screen that plainly had it — but only because that assertion happened to be written; every 9C embed had gone through the same helper with its footer invisible, and no emulator test had checked one. The helper now includes the footer and the 9C end-to-end test asserts its label too. Same shape as decisions 116, 188 and 192, caught one step earlier: a view that cannot show a property cannot be used to test for it. |
| 205 | 9D "both reached" is not an answer, and "neither reached" is its own | Scoring a published readout asks whether price covered the target quantile before the stop quantile. An evaluation records the maximum favourable and maximum adverse excursion, which settles three of the four cases outright — but when **both** distances were covered, candle data cannot order them. `mfe_at` and `mae_at` are the times of the *extremes*, not of the first touch of a quantile, so ordering by them would be wrong in a way that looks right; those are `unresolved` and excluded from the rate, exactly as §22 excludes PENDING. **Neither** covered is counted against the rate — the question is "did the target come first", and the answer is no — but in its own bucket, because "it went against you" and "it went nowhere" are different lessons and a merged rate would hide a target that was simply too far away. The rate is broken out per cohort key, since a cohort that had to widen is a weaker claim than an exact one. |
| 206 | 9D `Assessment.horizons` became `Assessment.confirmations` | A boundary test greps review code for `.horizons` to stop it reading `DetectionEvaluation.horizons` unfiltered — the guard that keeps a PENDING outcome from being counted as a miss (§22) — and the new scoring module tripped it on `assessment.horizons`, which is a different thing entirely. The field was renamed rather than the guard narrowed: `confirmations` is the accurate name (it holds confirmation rows, one per horizon, not horizons), and a blunt grep that cannot be fooled is worth more here than a precise one that can. |
| 207 | 9D the "last assessment" line on an order screen carries no prices | §64 allows `/execute` to show the last readout as an info line. It shows the bias, the cohort's n, the reached-rate and how many minutes old it is — and deliberately **not** the target and stop prices. Those are quantiles of past excursions, and a price printed on the screen where somebody is about to place an order is one copy-and-paste away from being treated as a level; `/monitor` shows them in a context that says what they are. The line also goes in its own `info` list rather than `warnings`, because a warning colours the embed amber and reads as "something is wrong" — putting it there would make every informed order look like a risky one. A failed read is swallowed: the line is context, and a readout that could not be fetched must not stop somebody placing the trade they had already decided on. |
| 208 | 11A F-3 the demo-account refusal in the drills had never once been able to fire | `demo_drills.py` read `bool(getattr(account, "is_demo", True))` — and `AccountInfo` has never had an `is_demo` field, in any phase. The expression was therefore always `True`, so `if not demo` was always False, and the guard protecting four order-placing drills and one that drops a connection mid-send was dead from the day it was written. Found by grepping for the field rather than by reading the line, which is why it survived: the code reads exactly like a working check. It now reads `account.mode is not AccountMode.DEMO`, refuses CONTEST and UNKNOWN as well as REAL, and names the login and server in the refusal. Planting the original expression back turns three tests red. A `getattr` with a default over a typed model is the shape to distrust: the default is a silent answer to a question the object cannot be asked. |
| 209 | 11A F-3 UNKNOWN is real money, everywhere, and the enum refuses to guess | `AccountMode.from_trade_mode` maps MT5's 0/1/2 and sends everything else — 3, `None`, a string, a broker that starts reporting something new — to UNKNOWN, and `is_real_money` returns True for UNKNOWN as well as REAL. A `dict.get(value, cls.DEMO)` would have been the natural spelling and is the one direction this mapping must never fail in. The asymmetry decides it: being wrong toward refusal costs one order and a confused minute, being wrong the other way puts an order on somebody's savings from a system whose tests have never seen a real fill. The same rule holds for a missing `AccountInfo`, a broker whose `account_info()` raises, and an unpublished `system_state.account_mode`. |
| 210 | 11A F-3 the live gate is an environment variable, and the guard's FIRST rule | `AUREON_ALLOW_LIVE_EXECUTION` is read from the box, never from Firestore, and only the exact string `"true"` unlocks it — `1`, `yes`, `on` and a leftover `false` all leave it locked. `trading_enabled` is a switch a human flips in Discord in a hurry; this is a line somebody wrote on the machine while looking at which terminal was open, and making it flippable from a phone would collapse two decisions into one. `rule_live_execution_allowed` runs ahead of `rule_trading_enabled`: when both are wrong at once, the switch is something the operator turned off on purpose and the account is something they may not have noticed, so reporting the switch first sends them to Discord to flip it — exactly the wrong next action. `check()`'s parameter defaults to False so a future call site that forgets it refuses rather than permits. |
| 211 | 11A F-2 the credentials check is a diagnosis; the round trip is the proof | Two rows, deliberately. `check_credentials` reads the variable and the file it points at, so a missing key is reported as a missing key and an OAuth client secret is named as the wrong file — both are JSON, both come from the same console, only one has `client_email`, and telling somebody "invalid credentials" sends them to re-download the same file. `check_firestore` still writes and reads back a document, because configuration that looks right and a round trip that works are different claims. The emulator makes the credentials row **SKIP, never PASS**: a green row for a check that never ran is how an emulator-only run comes to look like evidence about production. `is_default_credentials_error` walks the exception's `__cause__`/`__context__` chain and matches on the class NAME, so this module stays importable without the Google libraries — the same trade the notification repository makes for `AlreadyExists`. |
| 212 | 11A F-5 the profile fields are named `tick_volume`, not `volume` | `ProfileBin.volume` → `tick_volume`, `VolumeProfile.total_volume` and `ProfileSummary.total_volume` → `total_tick_volume`, the Discord row label "Volume" → "Tick volume", and a `TICK_VOLUME_NOTE` footer on every detection and reminder embed plus one line at the top of `/status`'s §19 block. The docstrings had said "estimated tick volume" since 9B while the FIELDS said `volume` — and a reader writing a query reads the field name. MT5's M5 candle reports the number of price CHANGES in the bar and says nothing about contracts traded or where inside the range they happened, so a POC under a label reading "Volume" invites exactly the interpretation a futures trader would give it. The caveat is stated once per block rather than once per row: three rows each carrying it reads as noise and gets skipped, which defeats it. No behaviour change; the only stored shape affected is `system_state`, which is rewritten every candle. Decision 168's text keeps the old field name because the decisions file is append-only history. |
| 213 | 11A F-10 the collection registry is derived, and the test derives it a SECOND way | `ALL_COLLECTIONS` was hand-maintained and had already drifted by two entries — `assessments` and `trade_notes`, both added in 9D and never listed — so the contracts table, the docs check and every cleanup routine silently did not know two live collections existed. Nothing failed; they were simply not considered. It is now generated by scanning the module's own namespace for values starting with `{PREFIX}_`, discriminating on the prefix rather than on naming convention so a doc-id constant like `LEGACY_SYSTEM_STATE_DOC` cannot be swept in (a cleanup iterating the list would try to delete a collection named `current`). The generated call sits at the END of the module, which leaves one hole: a constant defined below it would be missed. So `tests/unit/test_paths.py` re-derives the set by parsing the SOURCE with AST for `= collection(...)` — an independent derivation, because a test that scanned the same namespace would be asking the implementation to agree with itself. `gen_contracts.py` now iterates the registry and marks an undocumented collection in the table, which fails the committed diff. |
| 214 | 11A F-11 CLAUDE.md has told every contributor to read a file that has never existed | `docs/ARCHITECTURE.md` is named in CLAUDE.md's first line and in ten package docstrings, and is not in the repository — in any phase. Every "read the frozen spec before any change" instruction has therefore pointed at nothing, which is worse than no instruction: it reads as though the spec exists and somebody else has consulted it. The file has to be SUPPLIED; reconstructing §-numbered prose from memory would produce a plausible document nobody wrote and everybody would then treat as authoritative. `tests/unit/test_docs_consistency.py` asserts every doc CLAUDE.md names exists, marked `xfail(strict=True)` so it turns RED the moment the file lands — the signal to delete the marker rather than keep it. The same file enforces a hand-maintained banned-phrase list (`docs/DOCS_CHECK.md`) and that every collection in the registry appears in CONTRACTS.md, with `PHASE1_DECISIONS.md` exempt because rewriting a recorded decision to match the present would destroy the record of the change. |
| 215 | 11A F-15 the ops register is a stored document, and posts once per EDGE | Ten named conditions, each announced once on onset and once on recovery and never in between. A condition that persists for six hours would otherwise produce six hours of identical messages, an operator would mute the channel, and a muted channel is strictly worse than no channel because it looks like coverage. The state lives in `{prefix}_ops_events` rather than in a variable for two reasons: `/ops` runs in the Discord process while the conditions are detected in three others, and reading the stored row is what makes "once" survive a restart in the useful direction — a new process genuinely does not know whether a condition was already active, so a deploy mid-outage does not re-alert. One write per **transition**, not per poll: ten conditions polled every second across four processes would be forty writes a second for a value that has not changed, the same reasoning `system_state`'s throttle rests on. `since` records when the state BEGAN, so a three-hour-old onset reads as three hours old; a field that ticked forward on every poll would always read as new, and a test plants exactly that. Cleared rows are kept, because "flapped twice and cleared", "fired once and cleared" and "never happened" are three different things an operator needs to tell apart. Nothing gates on an ops event — a condition that should stop execution does it through the guard with a `FailureCode`, where it is testable. |
| 216 | 11A F-15 the two momentary-sounding events are phrased as conditions | `mt5_reconnect` is "the connection is currently being re-established", not "a reconnect happened", so it has a recovery: a moment with no clear edge would tell an operator a reconnect started and never that it worked, which is the half of the story that matters at 03:00. `live_account_detected` is "this process is on real money", which clears only if the account changes — and that is honest, because the condition really is still true. `symbol_feed_stale` is the one SCOPED event: silver's feed going quiet says nothing about gold's, and a single event for both would clear the moment either recovered, reporting healthy while one symbol was still dark. The name set is CLOSED (`SPECS`) and an unknown name raises: a register accepting any string would grow a second vocabulary of near-duplicates that no runbook could document. |
| 217 | 11A the bare-collection-literal boundary check had silently narrowed, and excluding by VALUE made it worse | `BARE_COLLECTION_NAMES` in the boundary test was a third hand-maintained copy of the registry, missing `assessments` and `trade_notes` — so the check that exists to catch a bare `client.collection("assessments")` could not catch one for the two newest collections. A boundary test that quietly narrows its own scope is worse than none: it reports green over exactly the ground it stopped looking at. Now derived from `paths.ALL_COLLECTIONS`. Widening it surfaced four innocent hits (`{"trade_notes": ...}` as a document key, `fields["trade_notes"]` as a subscript, `getattr(context, "assessments", None)`), and the first fix — excluding any string equal to a model field name — **suppressed a genuine `collection("assessments")` too**, caught by planting that exact call. The exclusion is now by POSITION: a dict key, a string subscript, and the attribute-name argument of `getattr`/`setattr`/`hasattr` are innocent; an argument to `.collection(...)` never is, whatever the string says. |
| 218 | 11B a close is confirmed over a window before anything acts on it | `AUREON_CLOSE_CONFIRM_SECONDS` (300) of unbroken CLOSED on **every** configured symbol, and any other reading inside the window resets the clock to zero rather than accumulating. A broker restarting, a symbol briefly disabled and a quote request that timed out into `trade_mode=unknown` each show up as one or two closed polls inside a perfectly normal Tuesday, and a service that tore down its loops on the strength of that would not merely cost a wake-up: the once-per-close work — the monitor's full reconciliation, the last daily review, both weeklies — would fire mid-session and the weekly reviews would be written against a week that had not ended. Five minutes of idle polling at the real close is the whole price. |
| 219 | 11B STALE and UNKNOWN never put a service to sleep | Only CLOSED sleeps. A feed that has stopped ticking inside trading hours, and a provider that cannot say what a symbol is, are the two moments an operator most needs the heartbeats fast and the ops register loud — and both are indistinguishable from Saturday if all you have is a tick timestamp, which is exactly why `MarketStateService` separates them in the first place (§10). Sleeping on a fault would convert an outage into two days of silence that reads as the weekend. Planted: treating "not OPEN" as closed turns two tests red. |
| 220 | 11B every configured symbol must be closed, and no symbols never sleeps | CLOSED on XAGUSD alone is a symbol being disabled, not a market closing, and parking the loops on it would stop observing XAUUSD while it is open and moving. `all_closed` therefore requires `bool(states)` as well: "all of nothing is closed" is vacuously true and would park a service for ever. The first version of the one-symbol test asserted only the phase at the END of a simulated hour and was **vacuous** — a machine that slept on `any` and was then woken by the same poll's OPEN symbol oscillates AWAKE → CLOSING → ASLEEP → AWAKE and lands on AWAKE by luck. The helper now asserts the phase, `parked`, `asleep` and `closed_since` at every step. Same failure mode as decisions 116, 145, 149, 159, 161, 188, 192 and 204. |
| 221 | 11B the wake is reported once, at the crossing out of ASLEEP | `WAKING` exists because being up *at* the open is too late — the observer still has a backfill to run and a cursor to seed — so services come back at the earlier of PREOPEN and `AUREON_PREOPEN_SECONDS` (900) before the scheduled open. The later `WAKING → AWAKE` step, when the first OPEN reading arrives, is therefore **silent**: reporting it as a second wake would re-run the observer's backfill against a live feed. `parked` is true for ASLEEP only, so a waking service polls and heartbeats at the awake cadence; `asleep` stays true through WAKING, because the market is still shut and no trade may execute. |
| 222 | 11B `next_open` is strictly after the moment asked about | At exactly Sunday 22:00 the market is open, so the next open is a week away, not now. The alternative reading gives a service that wakes at the open a wake deadline already in the past, and `_open_is_imminent` would then be permanently true. The weekly boundary is computed in UTC with no DST arithmetic anywhere — a broker whose clock observes summer time would otherwise move the weekend twice a year — and the weekday/time pair lives on `WeeklySchedule`, so a broker on a different calendar is configured rather than patched. No service asks the weekday itself. |
| 223 | 11B plant-and-revert needs bytecode writing off | A plant that swapped `all` for `any` **survived**: 24 tests passed against code that was visibly wrong. The cause was not the test — Python was running a stale `.pyc`. Its validation compares the source's mtime and size for EQUALITY, and a same-length edit reverted inside the same second matches both, so the cached bytecode for the planted version was reused. Every plant from here runs under `PYTHONDONTWRITEBYTECODE=1`, and the harness now fails loudly when an anchor is not found exactly once instead of letting the assertion's stderr scroll past while pytest runs against the untouched file. A verification technique that can silently verify nothing is worse than none. |
| 224 | 11B the four services sleep differently, and the differences are the design | Each holds its own `SleepCycle` behind a shared `SleepGate`, and each parks something different. The **observer** parks its candle loop, flushes the outbox and the parquet archive, saves the cursor and polls once a minute. The **executor** parks *nothing*: a closed market is already refused by the guard on this process's own broker, and a human who confirms a trade on a Saturday is owed that refusal rather than silence until Sunday night — only the cadence changes, and it can slow right down because the snapshot listener, not the poll, is what makes a CONFIRMED request prompt. The **monitor** does park, because every number it records comes from a quote and a closed market has no new ones; what makes that safe is one full reconciliation AT the close, the last chance to compare our books against the broker's while the broker is still answering. The **review watcher** does nothing but wait, then generates the last daily and every symbol's weekly — on the market's word rather than a cron line's, which is a fourth opinion about when the week ends and cannot notice a holiday. `SleepGate` never raises: a service that crashed because it could not work out whether the market was open would be strictly worse than one that stayed awake unnecessarily. |
| 225 | 11B a bar spanning the weekly close is refused, and excluded from the window rather than merely undetected | Its open is the last price before the weekend and its close the first price after, so its range IS the weekend gap: any cross found in it is a two-day move that involved no trading, and feeding it to the buffer would make every EMA wrong for the next `window_size` bars. So the analysis engine records a `SkippedCandle` and does not append it, which leaves the window contiguous in MARKET time — what the indicators are defined over, and what the replay fixtures already do. The observer asks the same predicate again rather than inferring "no detections" (a quiet candle and a refused one are different things), archives the bar anyway because §82 says the archive records what the broker SERVED and replay must reach the same guard on the same bytes, and advances both cursors or the bar would be refetched and refused for ever. The boundaries are exclusive: 20:55–21:00 on a Friday is five real minutes and refusing it would throw away the week's final candle every week. The predicate is injected rather than imported, so `aureon/engine` stays clear of `aureon/services`. Stated limit: a bar whose NOMINAL length runs past the close though its data stops there — MT5's Friday D1 — would be refused; unreachable here because the polled streams are intraday and the higher timeframes are aggregated from closed M5. |
| 226 | 11B the observer's shutdown flush had never delivered anything | `OutboxWorker.drain_once` checks the stop flag before every row so that `stop()` aborts a long batch promptly. The consequence nobody had noticed: `stop(); flush()` — the order `main_observer.shutdown` has used since Phase 2 — delivered **zero** rows, every time. The "delivered N detection(s) during shutdown" log line could not fire, and the module docstring's promise that "a clean stop leaves nothing queued" was false; every graceful stop left its queue for the next boot to drain, which is safe but is not what the code said. Found by writing a close-time flush test and reaching for `worker.stop()` to simulate an undelivered backlog. `flush(final=True)` now waives the stop flag and nothing else: the batch still halts at the first failing row, because one outage must not cost five rows an attempt each. The first version of that second test asserted only "nothing delivered, five still pending", which holds equally for a worker that hammered all five — planting `continue` for `return` proved it vacuous, and it now asserts the attempt count. |
| 227 | 11B `announce_account_mode` had never been called, and `AUREON_ALLOW_LIVE_EXECUTION` was inert | 11A F-3 wrote the executor's live-account announcement, tested it in isolation, and its own docstring said "called by `main_executor`" — while nothing called it. So `reconcile_only` was never set, the `live_account_detected` ops event was never posted, and `ExecutionWorker.account_mode` stayed UNKNOWN in every running executor. `build_executor` also never passed `allow_live_execution`, so the operator's deliberate escape hatch did nothing: the parameter's False default meant the failure was in the safe direction (a real account would be refused whatever the box said), which is exactly why it went unnoticed. Both are wired now, the announcement after `connect()` and before reconciliation — reconciliation runs whatever the account is, because a position already open needs watching whoever opened it. Third instance this phase of the same shape (decisions 208, 213): code that reads like a working check and is not reachable. |
| 228 | 11B Discord reads the phase and never computes it | `system_state` carries `sleep_phase` and `next_market_open`, published by the process that holds the feed. Discord may not call the broker (CLAUDE.md), so it has no way to classify a market and no business holding a second opinion about the weekly boundary — one source, one answer, and `/status`, `/execute` and the bot's own cadences all read it. Three consequences. The freshness thresholds widen to **twice the sleep cadence** while asleep: at the awake forty-five seconds, four services beating every five minutes read as four dead services from the first weekend minute, and a screen that cries wolf every weekend is a screen nobody reads on the Monday it is right — two missed sleep beats is still a hang, so the widening is not a disabling. `/status` prints the next open above the symbol rows, because "closed" alone reads as a fault and the first thing somebody does about a fault is restart something. And the bot's heartbeat and notifier cadences follow the published phase from a one-read-a-minute task; a read failure is skipped rather than acted on, since falling back to the awake cadence on every hiccup would undo the slowdown repeatedly. |
| 229 | 11B `/execute` is REFUSED on a confirmed close, where a closed symbol row is only a warning | Every other gate on the confirmation screen is a warning: the human decides and the guard refuses at execution time, which is right when the condition might clear inside the sixty seconds a confirmation lives — a stale feed, a wide spread, a symbol the broker briefly disabled. A confirmed weekly close will not clear for forty-eight hours, so the only thing a confirmation could do is expire: it would burn the human's confirm press, write a REQUESTED document that becomes an EXPIRED one, and teach them that Aureon's screens do not mean anything. The refusal lives in `plan_market_order`, so `/execute` and the [Execute] button on a detection get it from one place, and both now read the state document BEFORE planning. A **missing** state document is not read as closed: that is already reported by `/status` and stops the order a step later for a reason that names the observer, and refusing it here would be a guess dressed as a fact. `/remind` still arms, because a level somebody wants to know about on Monday is exactly the sort of thing to set up on a Saturday. |
| 230 | 11C F-1 Phase 2's evidence gate is per SYMBOL, and the symbol list comes from a committed table | The artefact globbed `evidence/session_*.md`, so ONE verified file turned the column green — and with two instruments observed, a verified gold session would have reported Phase 2 complete while silver had never been run. Thresholds are in points and points are different money per instrument (decision 141), so there is no partial credit: the cell stays ⬜ and names the symbols still owed. The symbol list is `symbol_tuning.known_symbols()`, not `AureonConfig.from_env().symbols` — which was the first version and was wrong in a way worth recording, because `AUREON_SYMBOLS` defaults to gold alone and running the tool on a box that had not exported it produced a gate demanding one session. An evidence gate is a claim about the project, so it reads a committed source; and a symbol has a reviewed tuning entry only because somebody chose its thresholds in that instrument's own money, which is exactly the population a gold session proves nothing about. 11A and 11B also got CATALOGUE entries, because a row absent from the catalogue keeps whatever its Evidence cell said — hand-written cells in a derived column are the weaker claim the column exists to replace. |
| 231 | 11C F-4 the tuning report measures what the AGENT measures, and has no `--apply` | For each threshold the report computes the distribution of exactly the quantity the agent compares against it, and names the agent's module beside it: a report on "wick size" that computed a slightly different ratio from `WickAgent`'s would be advice about a different number and would look identical. Two tests hold that — a bar the report says clears every wick threshold must be one the engine emits on, and a bar below `min_range_points` must produce nothing. It answers "what would this threshold admit", not "which value is best", because nothing here knows what a good detection is; `report_outcomes.py` is where that question lives. There is no `--apply` and there will not be: a threshold change is an `agent_version` bump (§12), and a flag that edited `OVERRIDES` would let somebody retune a live system by running a report, after which the detections either side would sit at ids produced by the same version — indistinguishable and incomparable. The guard is an AST check for the argument rather than a grep for the string, because a grep trips on the docstring explaining why there isn't one, and that is how a guard comes to be deleted for being annoying. First finding out of the tool: gold's `min_range_points = 20` admits ~99% of five-minute bars, so as a filter it does almost nothing. |
| 232 | 11C F-9 provenance is read from the evidence directory, because Firestore cannot tell | A replay run writes detections into the same collection a live session does, so nothing stored can distinguish a day the broker served from a day a fixture generated. `session_evidence.verified_market_dates` can, because the evidence file is written by `session_verify.py` after six checks against a real terminal and carries the commit it was verified at — and a file without the `SESSION VERIFIED` marker does not count, since one is written when a session STARTS. `Assessment` gains `history_source` (synthetic / real / mixed / unknown) and `real_days`, both stored rather than derived later: the answer depends on which sessions had been verified when the readout was published, and that is not recoverable afterwards. UNKNOWN is deliberately not SYNTHETIC — "nobody recorded it" and "we know it was generated" are different, and collapsing them would relabel every readout written before the field existed. An empty verified set IS synthetic, because the caller looked. MIXED gets its own label as the hardest to read silently: the rate is a weighted average of a real frequency and a generated one and nothing about it looks unusual. |
| 233 | 11C F-9 maturity is counted in DAYS, separately from the cohort floor | `MATURE_REAL_DAYS = 10`, and `immature` is independent of `insufficient`. Two hundred detections from one Tuesday are not two hundred independent observations: they share a session, a volatility regime and one day's news, so a readout can clear `MIN_COHORT = 30` and still rest on a single day — which is the common case early on. Ten days is the smallest stretch holding two of every weekday and both ends of a week. The caveat renders in the `/monitor` embed's DESCRIPTION, above the numbers, not in the footer: "every rate here describes the generator" is not something a reader should meet after they have already read the rates, the same reasoning behind decisions 188 and 204. The weekly review keeps its merged `assessment_hit_rate` and adds `assessment_hit_by_source` beside it, because the merged one is what a reader will quote and the split is what tells them whether to. |
| 234 | 11D every timeframe above M5 is AGGREGATED from M5, never fetched | One polled stream per symbol, and M15/M30/H1/H4/D1 derived from it. Parity decides it: replay feeds the same engine the same M5 bars, so an H1 bias derived from them is reproducible from the archive, where a broker's own H1 is its own aggregation and a difference between it and ours would be indistinguishable from a bug in either (§82). It also keeps one cursor — a second stream is a second thing to seed and a second place a gap can appear — and costs five fewer requests a second. A higher bar is emitted only when its bucket is **complete and contiguous**: an hour holding eleven of its twelve M5 bars has a high that may still be exceeded and a close that is not the close, which is the "price that never finally existed" the M5 grace period exists to prevent, one level up. Intraday buckets are anchored to the UTC day like `floor_to_timeframe`; D1 is keyed on the BROKER date, because a bar at 22:00 UTC belongs to the next trading day in Athens and a UTC-bucketed "day" would split one session across two. |
| 235 | 11D a daily bar is complete when a LATER day arrives, and the first version emitted the forming one | There is no fixed bar count for a broker day — a holiday, an early close or a late open makes any number wrong — so the only evidence available that a day has ended is a bar belonging to a later one. The first version checked contiguity alone and happily emitted the day still in progress, whose close is not the close: a daily bar looks plausible whatever is in it, which is what makes this worse than the same bug on an hour. The stated limit: this cannot tell whether the FIRST day in the input is partial, because a day missing its early bars looks identical to a day that opened late; `market_day_frames` stores whole archived days for that reason, and the cost of getting it wrong is one bar at the far end of a window where the EMA has least weight. |
| 236 | 11D the MTF tail is SEPARATE from the analysis window | `AnalysisEngine.window_size` is a correctness parameter: it is fixed at the hungriest agent's stated requirement precisely so that adding an agent cannot change another's detections, and widening it to reach H4 would silently rewrite every agent's history. So the MTF context reads its own bounded tail (`AUREON_MTF_M5_BARS`, default 2880 ≈ ten 24-hour days, enough for an H4 EMA(50)), and zero disables it — an engine that was not asked for the context keeps exactly the memory it had before 11D. D1 needs fifty trading days and will not be reachable from a live buffer at any sane size, which is what the `market_day_frames` cache is for. A timeframe with fewer than `slow` aggregated bars is left out ENTIRELY rather than given a half-seeded EMA: a recursive indicator depends on how much history it was given, so an EMA seeded on four bars is not a shorter version of the same number. |
| 237 | 11D a missing MTF read is not a flat one | `Detection.mtf` is `None` when the process had too little history, and `MtfContext.bias_of` returns `None` for a timeframe that was never read — where a timeframe that WAS read and had no view returns SIDEWAYS. Three states, kept apart: "nobody computed H4", "H4 had no opinion" and "H4 was flat" would all render identically if they were collapsed, and the first is about this deployment while the others are about the market. `alignment` gives three answers and no fourth, with MIXED covering both "some agree and some do not" and "nobody has a view": a SIDEWAYS timeframe is an absence of evidence, and counting it as agreement is how "aligned" comes to mean "we could not tell". A detection with no direction is always MIXED, because there is nothing for a bias to agree with. Nothing gates on any of it — whether alignment predicts anything is a question for the evaluation rules, and building a filter on the assumption that it does would be a threshold nobody researched. |
| 238 | 11D every agent's version moved, because the detection's content changed | ema_cross 2.1.0 → 2.2.0 and the other five 1.1.0 → 1.2.0, the same bump 9B made for the same reason. `mtf` is not in `DETECTION_ID_COMPONENTS`, so a detection with the context and one without would sit at the SAME id: indistinguishable and incomparable, which is exactly what D-1 put `agent_version` back into the id to prevent. Bumping forks the populations so a review can compare them. All six move together — one left behind would make its own detections silently incomparable across the change. |
| 239 | 11D only three timeframes are cached, and the day in progress is not written at all | `market_day_frames` stores M5, M15 and H1. M30, H4 and D1 are derivable from the stored M5 or H1 by `mtf.aggregate`, and a second stored copy is a second thing that can disagree with the first — the rule §59 already applies to indicators: store the source, derive the rest, never both. M1 is absent because tick-scale data never goes to Firestore (CLAUDE.md); it exists for reconstructing a position's excursions offline (§45) and lives in parquet. The observer writes a day only at the ROLLOVER, which is the moment it will never grow again and the same moment the parquet archive is flushed; writing per candle would be 288 writes a day for a document nothing reads until the day is done. A partial day is not written even at shutdown, a deliberate narrowing of what 11D asked for: it is only useful to a chart, and a document flagged `complete=False` is one mistake away from an aggregation reading it as finished. The bars are capped at 600 with `truncated` saying so, which turns a write that would fail at Firestore's document limit into a visible partial one — and the FIRST bars are kept, because a truncated tail shows up as a day that stops early where a truncated head would silently move the day's open. |
| 240 | 11D the `/status` panel prints a DASH for a timeframe nobody read | Not "sideways". The panel shows all five derived timeframes in one row, always present like every other row on it (a block that appeared only when populated would change the panel's shape as data arrives). A timeframe with no read prints `—` and one that was read with no view prints `side`: "nobody computed H4" and "H4 was flat" are different facts, and a status panel is exactly where a reader would otherwise conflate them. No alignment on the panel — alignment needs a direction and `system_state` describes a market rather than a signal, so publishing one would answer a question nobody asked. |
| 241 | 11D `mtf` is excluded from the live-vs-replay comparison, and the exclusion is a test | `scripts/compare_live_vs_replay.py` diffs whole detection documents, and 11D would have made every one of them mismatch: `mtf` is aggregated from a rolling MULTI-DAY buffer and the tool replays exactly one archived day, so a live detection at 09:00 on Tuesday carries an H1 read built from the preceding week while the same detection replayed from Tuesday alone has an hour of history and no H4 at all. That difference is entirely the buffer's depth — a deployment choice (`AUREON_MTF_M5_BARS`), not a correctness parameter — and reporting it as a parity failure would blame the engine for it. So `mtf` joins `schema_version` in `IGNORED_FIELDS`, with the reason in the module and a test that pins the entry so nobody removes it without reading why. The cost is stated rather than hidden: **this tool does not verify the MTF context.** The aggregation itself is deterministic and checked in `tests/unit/test_mtf.py` against buckets known by construction; what no one-day comparison can check is whether a live observer and a replay agree about a ten-day read. The 9B context stays in the comparison, because it is day-scoped and a one-day replay reproduces it exactly. |
| 242 | 11D the agent version pins live in tests, and regenerating the Phase 2 baseline is the proof `mtf` is context | Two tests carry the version as a literal (`test_part_b_agents`, `test_detection_context`), so a phase that changes the detection's shape and forgets one agent fails rather than shipping two document shapes under one id (§12). Deriving the expected version from the agent under test would pass for exactly the agent nobody bumped, which is the only failure the pin exists to catch, so the chore of editing it each such phase is the point. `docs/PHASE2_BASELINE.md` was regenerated for the bump and **only the version strings moved** — every agent's detection count over the fixture is unchanged, which is the evidence that attaching `mtf` changed what a detection records and not what any agent decides (decisions 234, 238). |
| 243 | 12 T-1 the launcher STOPS the stack on a child's death and never restarts it | A half-running stack is the dangerous state, and it is the one that looks healthy from the single screen an operator has: an executor with no monitor means positions nobody reconciles, an observer with no executor means confirmed requests that sit, Discord alone means a human confirming a trade that will never be claimed. So one child exiting logs `child_exited`, records `supervisor_stack_down` in `ops_events` so the fact outlives the terminal scrollback, stops the rest in reverse start order and exits non-zero for whatever supervises the launcher. Restart-with-backoff was in the earlier supervisor design and is deliberately absent: restarting a child that died on a poisoned document loops forever writing the same error, and restarting an executor mid-reconciliation is exactly how one unknown broker outcome becomes two. |
| 244 | 12 T-1 every child runs in its OWN process group, and the launcher is the only thing that signals them | `start_new_session` on POSIX, `CREATE_NEW_PROCESS_GROUP` on Windows. Without it the terminal delivers a console Ctrl+C to the whole foreground group, so every child starts shutting down at the same moment the launcher is also shutting them down in a deliberate order — and the order that actually happens is neither. One signal, one owner, a known order. On Windows the signal has to be CTRL_BREAK rather than CTRL_C, because `GenerateConsoleCtrlEvent` ignores the process-group argument for `CTRL_C_EVENT`; that arrives in the child as `SIGBREAK`, whose default action is to terminate, so every service now registers SIGINT, SIGTERM **and** SIGBREAK through one shared `aureon/services/shutdown.py`. Four hand-written copies of three lines is how one of them ends up missing the third. |
| 245 | 12 T-1 `supervisor_stack_down` is a CONDITION, not an announcement that something crashed | Phrased the way decision 216 phrases `mt5_reconnect`: it is true while Aureon is not running and clears when the launcher next starts the full stack, so the operator gets both edges. An event that only fired on the crash would tell them the stack went down at 03:14 and never that it came back at 03:20. It is the eleventh named condition, and the test that pins the set spells all eleven out rather than counting them — a count passes when one name is renamed and another added, which is how a runbook comes to document a name nothing posts. |
| 246 | 12 T-1 the `.env` is parsed by python-dotenv but validated more strictly than python-dotenv validates it | `load_dotenv` skips a malformed line and reports success. A dropped `AUREON_ALLOW_LIVE_EXECUTION=true` would then produce a stack that starts cleanly and a guard that refuses everything, with nothing anywhere saying why — so the launcher uses `dotenv.parser.parse_stream` directly and raises on a line that is not `KEY=value`, naming the file and line. `override=False` is the contract and not a default worth changing: the launcher hands its environment to every child, and a child re-reading the file with `override=True` could disagree with its parent about the collection prefix, which is the failure where half the stack writes to one database and half to another. The keys where the environment won are **named** in the log rather than counted, because "AUREON_FIREBASE_PROJECT_ID came from the environment, not from .env" is the one sentence that ends that debugging session. |
| 247 | 12 T-1 a child exiting ZERO is still a non-zero exit for the launcher | A service that stops by itself is not success — the stack is no longer whole. systemd reads a zero as "asked to stop" and leaves Aureon down until somebody notices, so `run` returns `exited.code or 1`. Planted as `return exited.code` and exactly one test fails, which is the test that exists for it. |
| 248 | 12 T-1 a killed child must be REAPED, not just signalled | Found by planting, not by review: `stop` sent SIGKILL and returned, so the launcher reported the stack stopped while the child was still a live (then zombie) process. SIGKILL cannot be caught, so the wait costs nothing and is the difference between "stopped" and "sent a signal at". |
| 249 | 12 T-2 `docs/ARCHITECTURE.md` is the AS-BUILT document and explicitly not the frozen spec | 11A refused to write this file, with a `xfail(strict=True)` whose reason was that reconstructing the spec from memory would produce a plausible document nobody wrote, which everybody would then treat as authoritative. T-2 resolves it the other way, and the difference is the source: written **from the code**, every claim checkable against a named file, and the one table that could drift generated. What it must never become is the thing 11A refused to produce, so its first paragraph says the frozen spec has never been in this repository and that the `§` numbers still point at a document the operator holds — and a test asserts that sentence is still there. The strict xfail is deleted rather than relaxed: a strict xfail that starts passing is a signal to remove it, which is what it said it was. |
| 250 | 12 T-2 the ownership table is DECLARED and checked against the derived registry, not derived itself | Collection names come from `paths.ALL_COLLECTIONS`, which is derived from the module's own namespace (11A, F-10). Who writes and who reads cannot be derived that way — it is a question about five entrypoints and a Discord package, and a static analysis that tried would be fragile in both directions. So `aureon/storage/ownership.py` declares it and `tests/unit/test_ownership.py` asserts the declared and registered sets are identical in BOTH directions: a collection added to `paths.py` with no entry fails, and an entry for a collection that no longer exists fails. Declaring it and then testing it immediately caught two wrong claims: Discord does not write `ops_events` (it only reads them for `/ops`), and `ops_events` is written by the observer and the launcher alone — `executor_stale` and `monitor_stale` are the observer's to report, because it is the process that reads the others' heartbeats. |
| 251 | 12 T-2 a docs check that greps the WHOLE document cannot tell a listed rule from a mentioned word | The first invariants test searched all of ARCHITECTURE.md for `assert_transition`, and a plant that removed the name from the invariant itself survived — the word still appeared in the Setups section three paragraphs later. The test now scopes itself to the numbered list under `## The invariants`. Its companion, which checks the other direction, first tried to pick "distinctive tokens" out of each CLAUDE.md bullet by punctuation and extracted the word "packages" from "reviews packages.", failing on a rule the document states perfectly well; a heuristic whose failure mode is a false alarm is worse than no test, because the next person deletes it. It is a declared fragment per non-negotiable, with the COUNT asserted, so a rule added to CLAUDE.md fails until the invariant is written. |
| 252 | 12 T-2 the Evidence column's 11A artefact is relabelled rather than satisfied | It said `the frozen spec` and pointed at `docs/ARCHITECTURE.md`, so the moment the as-built document landed the cell rendered `✅ the frozen spec` — a claim the document itself denies in its first paragraph. The label now names what the file is, its `must_contain` is the disclaimer sentence, and the frozen spec is a separate artefact pointing at a path that does not exist (`docs/FROZEN_SPEC.md`) so the table keeps saying it is owed. The guard test asks the table as a whole rather than 11A's row, because a row-specific assertion would have gone green by relocation. |
| 253 | 12 T-2 the claim-before-post tradeoff is pinned to the code by a test, not only described | A plant that reversed the document's description of it survived every other docs check: nothing connected the prose to `aureon/storage/notification_repository.py`. It is a real tradeoff — claiming first can LOSE a message, posting first can DUPLICATE one — and the direction was chosen because a duplicated price alert tells a human twice that a level they are watching has been crossed, which is a reason to act, twice. A document that quietly described the opposite would describe a system with the other failure mode. The test now requires both the document's "claims first" and the module's own heading, and that the document keeps naming the crash window it accepts. |
