"""Alembic revisions. One file per schema change, ``0001_initial`` first.

A package so the directory ships with the wheel; alembic itself locates revisions through
``alembic.ini``'s ``script_location``, not through this import.
"""

from __future__ import annotations
